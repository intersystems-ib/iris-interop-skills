<?xml version="1.0" encoding="UTF-8"?>
<!-- Rule:   BestPractices_Interop_IRIS.md §11.5 "OAuth 2.0 + LDAP server-side broker pattern"
     Validity: Still valid.
     Severity: High.

     This is a copy of %OAuth2.Server.Validate, customised to delegate ValidateUser
     to an LDAP / Active Directory bind sequence (anonymous bind -> StartTLS -> admin
     bind -> lookup UserDN by sAMAccountname -> rebind with the user's password).

     The original `^LDAP.MAC` example1 in [SAMPLES] is the canonical reference;
     this class inlines that sequence into the OAuth2 server hook.

     The companion class OAuth2.Server.Authenticate (customises the login UI;
     skips DisplayPermissions via btnAccept instead of btnLogin) is not included
     here - it's mostly CSP/HTML.

     Deployed as Alt.OAuth2.Server.Validate. `Alt` is the reserved package for system
     classes that had to be patched (skills/interop, "Reserved package names"); re-test
     it on every IRIS upgrade, because the patch may have to be reapplied.

     ALL VALUES ARE PLACEHOLDERS. Replace LDAPServer1/2, BaseDN and the Credentials
     entry name before deploying. The LDAP read user's DN and password live in
     Ens.Config.Credentials, never in this class source. The pattern
     (Init -> SimpleBinds(admin) -> SearchExts -> SimpleBinds(user)) is unchanged.
-->
<Export generator="Cache" version="25" zv="Cache for UNIX 2018.1.2 (Build 309_5_19198U)" ts="2019-11-22 15:35:41">
<Class name="Alt.OAuth2.Server.Validate">
<Description><![CDATA[
<initials><date>: A Copy of %OAuth2.Server.Validate to Customize the ValidateUser
Entry point and call an LDAP / Active Directory server.
The %OAuth2.Server.Validate class is the default ValidateUserClass which is included
with the server. The default class will use the user database of the Cache instance
where the authorization server is located to validate the user. The supported
properties will be issuer (Issuer), roles and sub (Username).
The ValidateUserClass is specified in the Authorization Server Configuration.]]></Description>
<IncludeCode>%syLDAP,%syLDAPFunc,%sySite</IncludeCode>
<Super>%RegisteredObject</Super>

<Parameter name="Credentials">
<Description><![CDATA[Name of the Ens.Config.Credentials entry that holds the LDAP read user:
Username = the bind DN (e.g. cn=svc-ldap-reader,ou=service-accounts,dc=example,dc=com),
Password = its password. The secret stays out of class source and is rotated from the
portal (Interoperability > Configure > Credentials) with no recompile and no re-export.
A class Parameter CANNOT be overridden by System Default Settings - SDS applies to
production host settings only - so the only supported override of the name below is a
subclass. Requires an interoperability-enabled namespace.]]></Description>
<Default>LDAP_READER</Default>
</Parameter>

<Parameter name="LDAPServer1">
<Description>Primary LDAP Server</Description>
<Default>ldap1.example.com</Default>
</Parameter>

<Parameter name="LDAPServer2">
<Description>Secondary LDAP Server. Not yet used.</Description>
<Default>ldap2.example.com</Default>
</Parameter>

<Parameter name="BaseDN">
<Description>The BaseDN for Searches</Description>
<Default>OU=Users,dc=example,dc=com</Default>
</Parameter>

<Parameter name="FILTER">
<Description>The attribute to search on (AD: sAMAccountname; OpenLDAP: uid)</Description>
<Default>sAMAccountname</Default>
</Parameter>

<Parameter name="UnixCertificateFile">
<Description>The Certificate file for SSL/TLS (Unix). Enables StartTLS when non-empty.</Description>
</Parameter>

<Method name="ValidateUser">
<Description><![CDATA[
<initials><date>: Customised ValidateUser - calls out to an LDAP Server.
All the LDAP Code has been copied from [SAMPLES]LDAP.MAC; look there for full comments.
Return 1 if username/password is valid, 0 otherwise.
If password="", the user is already logged into an existing session and the password
does not need to be validated.]]></Description>
<ClassMethod>1</ClassMethod>
<FormalSpec>username:%String,password:%String,scope:%ArrayOfDataTypes,properties:%OAuth2.Server.Properties,*sc:%Status</FormalSpec>
<ReturnType>%Boolean</ReturnType>
<Implementation><![CDATA[
	#DIM tValidUser as %Boolean
	#DIM tSC as %Status=$$$OK
	#DIM tLDAPErr as %Integer
	#DIM tLDAPMsg as %String
	#DIM errObj as %Exception.AbstractException
	#DIM LD   //LDAP Handle
	#DIM tFilter,tAttributes,tTimeout,tSizeLimit
	#DIM SearchResult, tNumEntries, tCurrentEntry
	#DIM tUserDN
	#DIM tCredentials as Ens.Config.Credentials
	#DIM tCredSC as %Status=$$$OK
	try {
		set tValidUser=0
		set sc=$$$OK

		///1- Bind against LDAP Server 1
		set LD=##class(%SYS.LDAP).Init(..#LDAPServer1)
		if (LD="") {
			set tLDAPErr=##class(%SYS.LDAP).GetLastError()
			set tLDAPMsg="ldap Init - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
			$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
		}
		///2- Turn on TLS Now if a certificate is configured.
		if (..#UnixCertificateFile'="") {
			set tLDAPErr=##Class(%SYS.LDAP).SetOption(LD,$$$LDAPOPTXTLSCACERTFILE,..#UnixCertificateFile)
   			if tLDAPErr'=$$$LDAPSUCCESS {
   				set tLDAPMsg="ldap_setoption(Certificate) - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
				$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
   			}
			Set tLDAPErr=##Class(%SYS.LDAP).StartTLSs(LD)
			if tLDAPErr'=$$$LDAPSUCCESS {
				set tLDAPMsg="ldap_StartTLSs - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
				$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
 			}
		}

		///3- Bind as the LDAP read user to get the UserDN for this username.
		///   Its DN and password come from Ens.Config.Credentials, not from class source.
	 	set tCredentials=##class(Ens.Config.Credentials).%OpenId(..#Credentials,,.tCredSC)
		if '$isobject(tCredentials) {
			set tLDAPMsg="Ens.Config.Credentials '"_..#Credentials_"' not found - "_$system.Status.GetErrorText(tCredSC)
			$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
		}
		set tLDAPErr=##Class(%SYS.LDAP).SimpleBinds(LD,tCredentials.Username,tCredentials.Password)
		if (tLDAPErr'=$$$LDAPSUCCESS) {
			set tLDAPMsg="ldap_Binds("_tCredentials.Username_") - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
			$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
		}
		///3.1- Search for the UserDN
 		set tFilter=..#FILTER_"="_username
 		set tAttributes=$lb("displayName")
 		set tTimeout=10,tSizeLimit=1
 		set tLDAPErr=##class(%SYS.LDAP).SearchExts(LD,..#BaseDN,$$$LDAPSCOPESUBTREE,tFilter,tAttributes,0,"","",tTimeout,tSizeLimit,.SearchResult)
 		if (tLDAPErr'=$$$LDAPSUCCESS) {
	 		set tLDAPMsg="ldap_SearchExt - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
	 		$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
 		}
 		set tNumEntries=##Class(%SYS.LDAP).CountEntries(LD,SearchResult)
 		if tNumEntries=-1 {
	 		set tLDAPErr=##Class(%SYS.LDAP).GetError(LD)
	 		set tLDAPMsg="ldap_Count_Entries - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
	 		$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
 		}
 		i tNumEntries=0 {
	 		$$$ThrowStatus($$$ERROR($$$GeneralError,"User "_username_" does not exist"))
 		}
 		i tNumEntries>1 {
	 		$$$ThrowStatus($$$ERROR($$$GeneralError,"LDAP Filter is not unique"))
 		}
 		set tCurrentEntry=##Class(%SYS.LDAP).FirstEntry(LD,SearchResult)
 		if tCurrentEntry=0 {
	 		set tLDAPErr=##Class(%SYS.LDAP).GetError(LD)
	 		set tLDAPMsg="ldap_FirstEntry - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
	 		$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
 		}
		set tUserDN=##Class(%SYS.LDAP).GetDN(LD,tCurrentEntry)
		#;A null password "binds anonymously" and returns success even though it does
		#;not match the user's password — reject it explicitly.
		if password="" {
			$$$ThrowStatus($$$ERROR($$$GeneralError,"ldap_Simple_Bind("_tUserDN_") - password cannot be null"))
		}
		//4- Authenticate the user based on their DN
		set tLDAPErr=##Class(%SYS.LDAP).SimpleBinds(LD,tUserDN,password)
		i tLDAPErr'=$$$LDAPSUCCESS {
	 		set tLDAPMsg="ldap_Simple_Bind("_tUserDN_") - "_##Class(%SYS.LDAP).Err2String(tLDAPErr)
	 		$$$ThrowStatus($$$ERROR($$$GeneralError,tLDAPMsg))
 		}else {
	 		set tValidUser=1
 		}
 	}catch (errObj) {
	 	set ^Demo.OAuth2Log($I(^Demo.OAuth2Log))=$zdt($h,3)_"|"_username_"|"_errObj.DisplayString()
		set tSC=errObj.AsStatus()
	}
	///5- Cleanup
 	i $d(SearchResult) d ##Class(%SYS.LDAP).MsgFree(SearchResult)
 	i $d(LD) d ##Class(%SYS.LDAP).UnBinds(LD)
	///6- Hand the diagnostic back to the OAuth2 server. ValidateClient below does the
	///   same with its `Set sc=$$$OK`. Without this line sc is never assigned, so every
	///   LDAP failure reaches the server as a bare 0 with no message for the operator.
	set sc=tSC
	quit tValidUser
]]></Implementation>
</Method>

<Method name="ValidateClient">
<ClassMethod>1</ClassMethod>
<FormalSpec>clientId:%String,clientSecret:%String,scope:%ArrayOfDataTypes,*properties:%OAuth2.Server.Properties,*sc:%Status</FormalSpec>
<ReturnType>%Boolean</ReturnType>
<Implementation><![CDATA[
	Set sc=$$$OK
	Quit 1
]]></Implementation>
</Method>

<Method name="SupportedClaims">
<ClassMethod>1</ClassMethod>
<ReturnType>%List</ReturnType>
<Implementation><![CDATA[
	quit ""
]]></Implementation>
</Method>
</Class>
</Export>
