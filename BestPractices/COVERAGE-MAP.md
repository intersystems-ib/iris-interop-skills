# Coverage map — canonical samples the plugin still needs

> **Status: living document. Multi-day project.** Update the ledger below as items land; do not
> rewrite the wave tables — they are the audit's output and their detail is the point. This exists
> so that building a sample is *following a recipe*, not re-deriving which sample is needed and why.

## How to use this

1. **Read the two systemic caveats** at the bottom of this header before picking anything up. They
   change what a green gate means for several items.
2. **Work a wave in order.** Wave 0 first — 16 of its items are not samples at all, and several
   remove a defect that a new sample would merely sit beside.
3. **Every new sample needs four things**, or it cannot reach COVERED: a `.cls` under
   `BestPractices/examples/` that compiles (tier 2), a row in `examples/README.md` (C4), a
   resolvable `/// Rule: §N.N "…"` header (C2 — and `§` means THIS deliverable; cite external books
   with the word "section"), and at least one skill pointing at it via `${CLAUDE_PLUGIN_ROOT}/…`.
4. **Rows marked ⚠ were corrected by review.** Read §"Review corrections" before building them —
   several are built on a premise the plugin itself contradicts.
5. **Record what you closed** in the ledger, with the release it shipped in.

## Progress ledger

| Closed | Item | Shipped in |
|---|---|---|
| ✅ | 3 dangling `BusinessRuleName` in `.cls` productions (`CensoRouting`, `AdtRouting`, `FhirRouting`) — rules written | v1.14.0 |
| ✅ | 4th dangling rule in `alert-circuit-production.xml` (`Example.Alerting.RUL.AlertRouter`) — rule written | v1.14.0 |
| ✅ | **N14** — tier-1 **C8**: production settings must resolve to a shipped class or an item in the same production, over `.cls` **and** `.xml` | v1.14.0 |
| ✅ | Wave 1 **S1** — HL7 routing rule with HL7 assist + `docCategory`/`docName` (CR-5, CR-6) | v1.14.0 |
| ✅ | **N2** — duplicate §1.7/§1.8/§1.9 headings (mine renumbered to §1.11/§1.12/§1.13) | v1.14.0 |
| ✅ | **N11** — `soap-bo` `%DeleteId({ID})` → `{Payload}` | v1.14.0 |
| ✅ | `snippets()` silent overwrite on duplicate class name — now fails and names the pair | v1.14.0 |
| ✅ | tier-1 **C9**: ratchets the ungated-fence counts, after the prose figure in `CLAUDE.md` was found stale (said 33, was 34) | v1.14.2 |
| ✅ | `update_snippet_baseline()` was dead code that would have clobbered any other baseline key — now merges | v1.14.2 |
| ✅ | **The Foundation-namespace recipe in `fhir/SKILL.md`** was the fence C9 caught — loose statements, compiled by no tier, so its three `HS.*` names were verified once by hand and nothing kept them verified. Given a host class (`Demo.UTL.FhirInstall`), so **tier 3 compiles it every release**. Signatures re-checked live first: `HS.Util.Installer.Foundation.Install(pNamespace, &pVars)`, `HS.FHIRServer.Installer.InstallNamespace(namespace = $namespace)` — which is why setting the namespace first is enough — and `InstallInstance(pAppKey, pStrategyClass, pPackageList, …)`. A host class rather than a bank artefact because the recipe belongs where the reader is; loose fragments 33 → 32. | v1.109.0 |
| ✅ | **N1** — the stale "No BPL, DTL or routing-rule example yet" paragraph. Every clause was false (1 BPL, 1 DTL, **5** rules) and issue #91 is closed | v1.15.0 |
| ✅ | **N3** — `alerting`'s inline function set was a defective fork of the gated file (bare `%Ensemble("SessionId")`, `kill …(day-1)`); replaced by a pointer + both traps named | v1.15.0 |
| ✅ | **N4** — `transformations`' `$ZSTRIP` mask. **Re-measured:** `"*-CWE"` raises `<FUNCTION>`; `"*CWE"`/`"*E"` return `""` for every input; `"*WC"` also strips **internal** whitespace (that last one was in neither skill) | v1.15.0 |
| ✅ | **N5** — `MyApp.Msg.` → `MyApp.MSG.`, **10 sites not 1**. `bpl`'s rule constraint and `business-operations`' `MessageMap` both named a class that did not exist, which matches nothing silently. Took tier 3 from 23/26 to 26/27 clean | v1.15.0 |
| ✅ | **N6** — `dicom`'s `NewStudy()` was `Quit $$$OK`: the endpoint 200s every POST and discards every study. Implemented, plus the `DICOM.MSG.StowRsReq` it needs, both now compiled | v1.15.0 |
| ✅ | **N7** — bare `[SqlProc]` wrapped in `MyApp.Bootstrap`; bare members 20 → 19 | v1.15.0 |
| ✅ | **N8** — `MyApp.Search.HL7` → `MyApp.Search.Hl7Adt` (C5 rejects a Tipo-last name; C5 never ran over snippets) | v1.15.0 |
| ✅ | **N9** — customer package name removed (5 sites) **and** the "verbatim from a running production" provenance line, per the vendor-neutral rule | v1.15.0 |
| ✅ | **N12** — all three cross-skill contradictions, each settled against the running instance: `GetValue` **does not exist** (it is `%GetValue`, 3rd arg is by-ref `pExists`, not a default); `bpl` taught the package-**qualified** rule call that cannot parse (`#5490`) and omitted `Document.`; `MessageSchemaCategory="2.5:ADT_A01"` **errors** — the category is concatenated with MSH-9, giving `2.5:ADT_A01:ADT_A01` | v1.15.0 |
| ✅ | `business-operations`' typed-SQL sketch was a loose fragment naming an unverified `$$$SqlDate`; replaced by a pointer at the gated bank file, whose `$$$Sql*` macros tier 2 does compile | v1.15.0 |
| ✅ | **N13** — the three indexed-but-uncompiled artefacts (`.xml`, `.cls.xml`, `.sh`) now carry ⚠️ on their own README rows | v1.15.0 |
| ✅ | 4 gate defects found while doing the above: a `///` prefix was split off its class and dropped; an `Include` was **always** orphaned (so `$$$Str2MsgTyp`/`$$$Sql*` fences compiled without it); an **indented** class fence was silently counted as a fragment; tier 3 left `Ens_Config.SearchTableProp` rows behind, so any SearchTable **rename** failed with `PropCollision` naming a deleted class | v1.15.0 |
| ✅ | **N15** — **tier 2b** compiles `BestPractices/external/**` with its own baseline. Surveyed first: all **19** classes compile clean today, so gating was free. Deliberately NOT added to tier 1 — **0 of 19** carry a `/// Rule:` header, so C1 would fail all of them and greening it would mean editing vendor source. Count correction: the audit said "21 classes / 1738 lines"; measured twice independently it is **19 classes / 1934 lines** | v1.15.1 |
| ✅ | **N16** — `scripts/issue_fingerprint.py` + `scripts/test_issue_fingerprint.py`, wired into the `test-hooks` CI job. **Not** a pytest: this repo's harnesses are stdlib-only so CI needs no dependencies, and matching that beat following the map's wording. The tests caught **4 real bugs** in the helper before it shipped | v1.15.1 |
| ✅ | **Wave 0 COMPLETE** (v1.15.1) | — |
| ✅ | Wave 1 **S3** — `msg-persistent-leftmost.cls` + `tdd-message-own-extent.cls`, new deliverable **§5.10**, and the **4 bank message classes that taught the shared-extent shape are fixed**. Measured: `(%Persistent, Ens.Request)` → own extent; `(Ens.Request, %Persistent)` and bare `Ens.Request` → `^Ens.MessageBodyD`. All three compile clean, so tier 2 was green on all three. Test mutation-checked: passes as shipped, fails on both wrong orders | v1.16.0 |
| ✅ | Wave 1 **S4** — `msg-persistent-child-delete-cascade.cls` + `dat-address-persistent.cls` + `tdd-delete-cascade.cls`, new deliverable **§5.11**. **The row's premise was wrong:** it said `%SerialObject` "has no such method" — measured, `%DeleteId` DOES exist on `%SerialObject`, compiles, and returns `#5753 Cannot instantiate abstract class` at runtime, which the snippet's `Do` then **discarded**. Found a 4th defect the map never listed: an unguarded trigger makes childless carriers undeletable. Test RUN and mutation-checked against all three | v1.17.0 |
| ✅ | **Class-based (non-BPL) `Ens.BusinessProcess`** — the corrections' second unproposed component type ("the whole map treats BP as BPL"). `bp-class-based-async.cls` + deliverable **§5.12**. Verified in a RUNNING production, both variants: with `pResponseRequired=0` the operation runs and returns, `OnResponse` is **never entered**, and the BP completes successfully — the reply is dropped in silence. With `1`, all four callbacks fire. Also left the container clean: a probe production from a failed first attempt had been left RUNNING and was found and stopped | v1.19.0 |
| ✅ | **Pass-through / `Ens.StreamContainer`** — the corrections' third unproposed component type, and the one S2 depends on. `production-file-passthrough.cls` + `bo-stream-container-consumer.cls` + `tdd-stream-container-rewind.cls` + deliverable **§6.10**. Confirmed first: **0 hits** for `Ens.StreamContainer` in `skills/` and 0 in the bank. Executed: `read1=[line-1] read2=[] afterRewind=[line-1]` — a second read returns "" with no error, which is how a relay writes an EMPTY output file from a run that reported success. Also gave `component-map` its **first bank pointer** (it cited zero) | v1.20.0 |
| ✅ | Wave 1 **S2** — `production-alert-circuit.cls` replaces the §7.1 `.xml`, and `alert-routing-rule.cls` now carries the real dedup conditions. **The conversion found the XML was unloadable**: both its dedup conditions used the package-qualified FunctionSet form. Compiled both ways — bare `AlreadyReportedPerSession()=1` COMPILES, `##class(...).AlreadyReportedPerSession()=1` FAILS with `<Ens>ErrParsingExpression`/`ErrInvalidToken`. Its own header told the reader to `$system.OBJ.Load()` it | v1.21.0 |
| ✅ | tier-1 **C10** — a rule's `<send target>` must resolve to an item in the production it names, and a non-empty `production=` must be shipped. Building it immediately found `routing-rule-fanout` naming `Example.Production`, which does not exist (all shipped ones are `Example.Productions.*`) | v1.21.0 |
| ✅ | Wave 1 **S5** — `dtl-hl7-symbolic-paths.cls` + `tdd-hl7-fixture-test.cls` + deliverable **§2.11**. The fixture dependency of S1/S12/S36. Measured: a DTL compiles **identically** with a correct or a nonsense symbolic path, so tier 2 is green on both. The map said a guessed name returns `""` with `$$$OK`; **measured it is subtler** — an invalid path returns `""` with an ERROR status, a valid-but-empty field returns `""` with OK, and the two-arg `GetValueAt(path)` everyone writes DISCARDS the only discriminator. Also: `ImportFromString` leaves DocType EMPTY; the component is `.IDNumber`, not `.ID` | v1.22.0 |
| ✅ | A **fourth** site of the `MessageSchemaCategory` colon-form defect — `transformations:322` — found while writing S5. v1.15.0 fixed three; this one survived | v1.22.0 |
| ✅ | **Name HL7 segments and fields, never number them** (user-requested policy). Promoted in the deliverable §2.11, `transformations` (new §"Name the field, do not number it"), `hl7-schemas`, `bpl`, `component-map`, `unit-tests` — and applied to the bank: the gated HL7 rule now compares `{MSH:MessageType.MessageCode}`, not `{MSH:9.1}`. Measured mapping table, plus three rules: you **cannot mix** (`{MSH:MessageType.1}` is invalid), names are case-insensitive, and `GetFieldNameFromNumber` is authoritative but returns **empty for repeating fields** | v1.23.0 |
| ✅ | **Wave 1 premise verification pass** — every remaining row's checkable claim measured on a live instance before building. Found **3 wrong** (`interop` says `Ens_Config.Item` is not queryable — it IS; the review correction that followed from it is void; `component-map`'s `Post(.resp,url,body)` takes no URL) and **1 stale** (S7) and **1 mis-stated arity** (S13). Confirmed S9, S10, S8. Recorded as §"Wave 1 follow-on", with a dependency-ordered execution list | v1.24.0 |
| ✅ | Wave 1 **S12** — `tdd-hl7-router-validation.cls`, the two-direction router test, RUN against a live production. **The conversion found the bank's own productions could not express the second direction:** `BadMessageHandler` named the SAME item the rule sends valid messages to, in BOTH `Hl7Intake` and `CensoIntake`. Measured before the fix: valid, unknown-type and malformed messages ALL routed to `BO.AdtOut`. After adding distinct sinks: valid → `BO.AdtOut` with no alert, both bad shapes → `BO.AdtBad` with an alert | v1.25.0 |
| ✅ | tier-1 **C11** (BadMessageHandler must not be a rule `<send>` target) and **C12** (no `--` inside an XML comment, #6301). C11 found the second collision immediately; C12 exists because I hit the `--` trap `hl7-schemas` documents and it cost a tier-2 round trip, presenting as an unrelated DTL failure | v1.25.0 |
| ✅ | Wave 1 **S6** — `utl-functionset-final.cls` + `dtl-lookup-and-functions.cls` + `tdd-dtl-function-forms.cls` + deliverable **§5.13**. All **six** call forms compiled AND run: in a DTL a built-in takes `..` and a custom function takes `##class()`; in a business RULE the same custom method takes **bare** and `##class()` fails at compile. The contexts are **opposite**. The bare form in a DTL is the dangerous one — compiles clean, `<UNDEFINED>` only when a message flows | v1.26.0 |
| ✅ | **`DependsOn` is mandatory on a DTL** — measured: same batch, no DependsOn FAILS, with it all compile. `dtl-order-to-vendor` shipped without it for releases and failed the moment an unrelated error perturbed the batch. Fixed, and tier-1 **C13** now enforces it | v1.26.0 |
| ✅ | Wave 1 **S10** — `bs-recordmap-service-subclass.cls` + deliverable **§6.11**. **Both the row AND the review correction were wrong** about #5478: measured, the ARITY is not enforced (dropping `pHint` compiles), the argument **types** are, and `pHint` by value instead of ByRef also compiles. `Ens.BusinessService` enforces nothing at all. The genuinely silent half is `##super()`: `%Dictionary.CompiledMethod.Origin` shows `RecordMap.Service.Standard` and `HL7.Service.FileService` define their OWN `OnInit`, so an override without `##super()` replaces real setup and compiles clean | v1.27.0 |
| ✅ | tier-1 **C14** — an `OnInit` override on a prebuilt `EnsLib.*` host must call `##super()`. It fired on the BASELINE: `ch03_cda/soap-messagebody-hl7-operation.cls` had shipped overriding `EnsLib.HL7.Operation.SOAPOperation.OnInit` without it, and that class defines its own `OnInit` (Origin = itself). Fixed | v1.27.0 |
| ✅ | Wave 1 **S11** — `bo-rest-outbound.cls` + deliverable **§6.12**. **All three of the row's premises were wrong, and a fourth error surfaced:** `EnsLib.REST.OutboundAdapter` **does not exist** — named in `component-map`, `soap-bo` and `business-operations`, twice as the first choice. Measured: every REST outbound host (`EnsLib.REST.Operation`, `.GenericOperation`, `EnsLib.HTTP.GenericOperation`) declares `EnsLib.HTTP.OutboundAdapter`. Also: `Post` takes **no URL** (`PostURL` takes it first); narrowing the `Adapter` property gives **no** compile-time checking (all four variants compile); and a non-2xx **does** fail the `%Status` — so the risk is a caller discarding it, not a lenient adapter | v1.28.0 |
| ✅ | Wave 1 **S9** — `sql-bo-batch-transaction.cls` + deliverable **§6.13**. Premise confirmed (no `StartTransaction`), and one refinement: `SetAutoCommit`/`Commit`/`Rollback` all originate in `EnsLib.SQL.Common` (**portable**) while the `Transact(type)` I found in the verification pass originates in `EnsLib.SQL.CommonJ` — **JDBC only**. `business-operations` said the API was "whole"; narrowed to "portable" | v1.29.0 |
| ✅ | Wave 1 **S7**, RE-SCOPED per V4 — the row contrasted a working `NormalizeKey` against "the tier-3-green broken one", which wave-0 N4 fixed in v1.15.0, and v1.26.0 then shipped the working version. What was still missing was a lookup against an **actually-loaded table**: `tdd-lookup-normalize.cls`, and §5.13 extended. Measured: normalised key → `DIA` with `exists=1`; raw key → `""` with `exists=0`; and via the FunctionSet the raw key returns the **DEFAULT** (`UNKNOWN`) — a plausible code, not an error. Also pinned: a genuinely empty `DataValue` returns `""` with `exists=1`, so only `exists` separates a miss from an empty value | v1.30.0 |
| ✅ | Wave 1 **S15** — `drift-report-disk-vs-namespace.cls` + deliverable **§1.14**. Premise confirmed EXACTLY: `Continue:$Extract(k, 1, n) '= (pPkg _ ".")` gives `#1012: Expected EOL or spaces` and the unspaced form compiles — a postconditional ends at the first space. Both lies executed: unmounted `src` → `ONLY IN IRIS: <class>`; wrong prefix → **`in sync`** having compared nothing, so an empty comparison and a clean one are indistinguishable. It had lived as **bare `ClassMethod`s** no tier compiled — bare fences 19 → 18 | v1.31.0 |
| ✅ | Wave 1 **S13** — `unittest-sqlproc-result-reader.cls` + deliverable **§5.14**. Row said "3-arg `DebugRunTestCase`": measured it takes **five** and **persists nothing at all** — no rows in `%UnitTest_Result.*`, `^UnitTest.Result` not even defined. `RunTest(suite, qspec)` with `^UnitTestRoot` does. The defect reproduced EXACTLY: every method gets a trailing `LogMessage` assert with Status=1, so an assert-scanner returns **`passed=2 failed=0`** for a case whose own Status is 0 — and **`passed=0 failed=0`** when nothing ran. Read `TestMethod.Status` | v1.32.0 |
| ✅ | Wave 1 **S8** — `production-preflight-validator.cls` + deliverable **§1.15**, buildable as originally written once V2 established `Ens_Config.Item` IS queryable. All three defects measured: it returned `OK` from three not-found paths; it matched the generic router class only, never the HL7 one; and `TOP 1` picked **`Ens.Alert`** in BOTH gated productions — including the HL7 one, where the real router exists and is a different class. Verdict is now three-valued and `OK` carries its counts | v1.33.0 |
| ✅ | **The corrected validator found a real defect on its first run:** FIVE of six gated productions carried an `Ens.Alert` item with **no** `BusinessRuleName` — which captures every `Ens.AlertRequest` and drops it, strictly worse than omitting the item. All five removed; tier-1 **C15** now refuses it | v1.33.0 |
| ✅ | Wave 1 **S16** — `tdd-destination-assert.cls`, the API that makes **CR-13 performable**. The row said `interop` and `bpl` contradicted each other about `Ens_Config.Item` and "nothing compiles either"; V2 settled it (the table IS queryable) and this ships the read. Both routes asserted to agree. Three distinctions pinned, all executed: `Target` is half the destination (Host vs Adapter, silent when wrong); an **absent** setting reads identically to an empty one, so found/not-found is the signal; and the read needs a REGISTERED production, so zero items means "I read nothing". `NameDestination()` is public so a reviewer can perform CR-13 in one call | v1.34.0 |
| ✅ | Wave 1 **S14** — `oauth2-server-validate-ldap.cls`, a **subclass** of `%OAuth2.Server.Validate`, replacing the 207-line `.cls.xml` copy that no tier compiled. The row's `%syLDAP` worry is void: `Include %syLDAP` **alone** resolves in an application namespace (`$$$LDAPSUCCESS`=**0** — so `If tErr` is the error test — `$$$LDAPSCOPESUBTREE`=2, `$$$LDAPPORT`=389, `$$$LDAPOPTXTLSCACERTFILE`=24578), and all **13** `%SYS.LDAP` methods called exist with the arities used. The copy's real cost, measured: it overrode `SupportedClaims` with `quit ""` — **6 claims → 0** — and declared a **five**-formal `ValidateUser` against the vendor's six. That stale signature compiles clean as a subclass, raises nothing when the server calls it with six arguments, and returns a plausible boolean; `use2fa` can then only ever read 0, with a positive control (a 6-formal override setting it to 1) proving the flag CAN carry. Also found by reading the vendor source: `password=""` means "already authenticated, skip the check" upstream and "**bind anonymously and succeed**" against LDAP, so the same convention inverts. **The bank's last `.cls.xml` is gone — C8 has no XML left to inspect** | v1.35.0 |
| ✅ | **Wave 1 COMPLETE** (v1.35.0) | — |
| ✅ | **Wave 4 premise verification pass** — all **18** rows measured before building any, the fourth such pass, with IDs assigned **S42–S59** (41 + 18 = 59, the map's own total). **Topic selection right in all 18 again; seven rows needed a correction.** The one that changes what gets built: **S51's gap does not exist** — the row says the `.DAT` package has "zero instances in the bank" and `dat-address-persistent.cls` declares `Example.DAT.Address` (Y1), the same shape as X4. **S48's row is right and the skill it cites is wrong** — `OnPreWebMethod` is a `%SOAP.WebService` callback on `EnsLib.SOAP.Service` itself, not something `EnsLib.SOAP.InboundAdapter` provides, so `business-services:757`'s stated prerequisite is false; and every platform `OnPreWebMethod` declares **no return type**, leaving the row's default-deny mechanism unverified until it is measured behaviourally (Y2). Two facts resolved: the suspect spelling really is wrong — **`NumberOfDaysToKeep`**, not `NumDaysToKeep` (Y3) — and S44 is sharper than written: a `list Of %String` with no `SQLPROJECTION` projects **no child table at all**, so `SELECT … ObsSummary_Observations` gives `SQLCODE -30` (Y4). **S56 confirmed exactly, including an upstream defect:** the vendored DICOM production's `DuplexTargetConfigName` value `DICOM STOWRS Process` names **no item** among its 17, and tier 2b compiles it clean (Y5). S57's mechanism holds but there is **no `$$$Pending` macro** — the vendored code uses the literal `65281` (Y6). S58's facts confirmed with a count drift (**six** headers, not five) and it is **recommended for decline**, since six independent headers already carry the rule and a bank file containing a `Storage` block invites the copy-paste they exist to prevent (Y7). S43 needs a fixture decision the row does not mention — all four RecordMap fields are `%String`, so nothing can be dropped for a type reason today (Y9). Eight rows confirmed exactly (Y8), and the two DICOM rows' standing ⚠ is cleared now §15 exists | v1.60.0 |
| ✅ | Wave 4 **S59** — **three added lines in the gated `tdd-testproduction-dtl.cls`**, as the row preferred, plus an extension to deliverable **§5.9**. No new file. **The obvious guard was measured and rejected:** "is this namespace Interoperability-enabled?" is useless, because on this IRIS for Health image `USER` carries `Ens.Director`, `Ens.Production` AND `Ens.Config.Item` exactly as the interop namespace does — a `USER` run passes that test. So the guard asserts against the **instance default** instead: `USER` is what you get when nobody passed a namespace, it exists on every instance, and it converts the exact reported failure into a red. **Proved by differential, not by mutation:** the same suite compiled and run in two namespaces gave `FHIRTEST run=490 methods=4 failed=0` and `USER run=3 methods=4 failed=1 -> AaNamespaceIsNotTheInstanceDefault`. One red, and it is the guard — **the other three methods PASSED in `USER`**, which is the finding rather than an aside: the transform assertions are right about the code and silent about the place. The guard is named to sort FIRST, because a red after eight greens reads as a flake, and it carries a positive control that the forbidden namespace EXISTS, or a pass could mean the name was meaningless. The row's honest limit stands and is stated in both the artefact and the section: this cannot pin a tool-call parameter and cannot catch a wrong-but-plausible namespace — it catches the default, which is the mistake that actually happened. | v1.86.0 |
| ✅ | **S51 and S58 formally DECLINED**, closing wave 4. **S51** (`msg-with-dat-serial-payload`): Y1 measured that the gap does not exist — the row says the `.DAT` package has "zero instances in the bank" and `dat-address-persistent.cls` already declares `Example.DAT.Address`. Building it would add a second sample for a covered point. **S58** (`msg-with-generated-storage`): Y7 measured **six** independent headers already carrying the no-hand-written-`Storage` rule (the row said five), and a bank file containing a `Storage` block invites exactly the copy-paste those headers exist to prevent — the counter-risk outweighs the illustration. Both are decisions, not deferrals. | v1.86.0 |
| ✅ | Wave 4 **S57** — `bp-mwl-findresponse-sequence.cls` + `tdd-mwl-findresponse-sequence.cls` + deliverable **§15.7**. Y6 held on both counts: the mechanism is real, and **`$$$Pending` does not exist** — re-measured, along with `$$$Success`, `$$$PendingKeys` and `$$$DICOMStatusPending`, all `MPP5610`. The sharp contrast is that **`$$$MsgTyp2Str` DOES compile** from the same include: `EnsDICOM.inc` names the *command field* and says nothing about the *status*, so the literal is the only option and the sample names it in a parameter. Read from the vendored `WorkListProcess` dispatch loop, the terminal reply is sent **outside** the row loop and unguarded, so zero rows still gets one — confirming the row's "including for zero rows" from the code rather than from the prose. Two protocol details added: **65281 is `0xFF01`** (the *other* Pending, `0xFF00`, exists too, so a responder that RECEIVES replies must accept both — the oracle records the asymmetry), and the terminal test must be **"not Pending"** rather than "equals Success", because a refusal (`0xA700`) or failure (`0xC000`) also ends the query. **Mutation 1 justifies the whole suite:** guarding the terminal on `pRowCount > 0` is caught by the zero-row test and by nothing else. Two compile traps met on the way in, both `#5559`: `Include` must precede the `Class` statement, and a `/* */` block inside a `///` header line is counted by the class parser. | v1.85.0 |
| ✅ | Wave 4 **S56** — `production-dicom-query-retrieve.cls` + `bp-dicom-leg.cls` + `tdd-dicom-query-retrieve.cls` + deliverable **§15.6** + **a new tier-2b gate check**. Y5 re-confirmed exactly (17 items, five `DuplexTargetConfigName` values, four resolve, `DICOM STOWRS Process` dangles) and measurement went further in three ways. **(1) The defect is a broken ROUND TRIP, not just a dangling name:** the item that exists is `DICOM StowRs Handler` — a rename near-miss in both casing and final word, so even a case-insensitive check misses it — and since `DICOM StowRs Handler`'s own `OperationDuplexName="DICOM Store DCM TCP Out"` DOES resolve, the STOW-RS leg can send and never receive. **(2) There is a SECOND item-reference setting the gate never checked:** `OperationDuplexName`, which is **not a platform setting** — zero classes on the instance declare such a property; it is the workshop's own convention via `Parameter SETTINGS` plus a `Property`. C8 and the new scan now share one `ITEM_REF_SETTINGS` constant, so the bank and the mirror cannot drift apart; all three vendored `OperationDuplexName` values resolve, so the coverage came free. **(3) Tier 2b now ratchets dangling item references in the vendored tree** — recorded in `external_baseline.json` under `known_dangling`, because it is a read-only mirror. **The check was positive-controlled both ways:** it flagged the upstream defect as `NEW` and FAILED the tier before any baseline existed, and after recording it, injecting a second dangling value (restored, sha-verified) made it fail again. Two harness corrections: **`Ens.Config.Setting` has NO SQL projection** (`SQLCODE -30`), so a join returned "" for every item and the suite said "four items carry a DuplexTargetConfigName, found 0" — walk `Ens.Config.Item.Settings` instead; and the upstream-defect assertion was **removed** from the IRIS-side suite because the gate container has **no bind mount** of the repo, so it could only fail environmentally or be skipped. | v1.84.0 |
| ✅ | Wave 4 **S54** — `fhirsql-setup-existence-test.cls` + `tdd-fhirsql-setup-existence.cls` + deliverable **§4.13**, plus corrections to **§4.9** and `skills/fhir`. **The row's conclusion is right and its stated reason is wrong**, and Y8 repeated the error: tier 2 does **not** excuse `#5373` — it ends `ok = not failed`, so any failure fails it. The excuse is in **tier 3**, the skill-snippet tier. The real reason a call-through cannot be an existence test is stronger: measured five ways, `##class(Absent).Method()` and `##class(Present).NoSuchMethod()` both **compile clean**, while `#5373` is raised **only** by a class in a TYPE position (property, formal spec, superclass). **And pinning the surface found two errors in shipped guidance:** §4.9 and the skill both said `Setup("/data/fhirdata/")`, but `Setup` takes **three** parameters (`dir`, `pNamespace`, `pStrategyClass`), returns **void** (so assigning it raises `<COMMAND>`, §6.21), and its `dir` default is an InterSystems **build** path — while **`LoadFHIRData`** is the method whose `dir` names data. Class parameters `FHIRNAMESPACE = "DEMO"`, `APPNAMESPACE = "HSSYS"`. Neither method was executed (FHIR SQL Builder is not installed here) and the section says so. **Mutation-checking caught two inert guards:** dropping either comma guard in `ParamCount` left the suite green, because no real FHIR SQL spec has a comma inside a quoted default or inside parentheses; the constructed case fixed that, and the FIRST attempt at it put every comma inside quotes, exercising only one of the two — the depth guard needed `%String(MAXLEN=10,TRUNCATE=1)`. | v1.83.0 |
| ✅ | Wave 4 **S53** — `oauth2-client-pkce.cls` + `tdd-oauth2-client-pkce.cls` + deliverable **§11.14**, and a rewrite of `skills/security`'s one-line PKCE entry. Y8 held (five `%SYS.OAuth2.*` classes, no FHIR blocker) and the row's claim holds as a protocol fact. **What measurement added:** IRIS's only PKCE-named method, `%SYS.OAuth2.Authorization.MakeCodeVerifier()`, is **declared Private** — `<PRIVATE METHOD>` from caller code — and **no public method** on that class mentions a challenge or verifier in its name or formal spec, so both halves of the client computation are yours. The challenge must be **base64url unpadded**: measured, plain `Base64Encode(sha,1)` is 44 chars with `/` and `=`, all illegal in a `code_challenge`; and the hash argument order is load-bearing (`SHAHash(256,text)` correct, `SHAHash(text,256)` raises `<ILLEGAL VALUE>`). The parameters travel through the `&properties` array both `GetAuthorizationCodeEndpoint` and `MakeAuthorizationCodeURL` accept. **Explicitly not measured:** server-side enforcement — `OAuth2.Server.Configuration` has **zero rows** and the config classes (`OAuth2.Client`, `OAuth2.Server.Configuration`, `OAuth2.AccessToken`) exist **only in %SYS**, all absent from the interop namespace, so `MakeAuthorizationCodeURL`'s `client As OAuth2.Client` names a class this namespace cannot see and the dangling reference compiles anyway (§6.20). **The test vector was measured, not quoted:** a recalled RFC 7636 Appendix B pair did NOT reproduce, and validating the primitive against `sha256("abc")` — byte-identical to an independent implementation — showed the recalled constant was the error, not the code. **And the first baseline was RED with the bug in the sample:** `VerifierIsWellFormed` ended `Quit i = tLen`, true both when the loop completed and when it broke on the LAST character, so a verifier ending in `/` was accepted; mutation 6 reinstates that line. The gate caught the postconditional-space trap **twice** in one file (`#1054` then `#1012`), after which every postconditional in it was audited rather than fixed one compile at a time. | v1.82.0 |
| ✅ | Wave 4 **S55** — `fhir-bundle-transaction-guard.cls` + `tdd-fhir-bundle-transaction-guard.cls` + deliverable **§4.12**, and a rewrite of `skills/fhir`'s Bundle sentence. Y8 held: the skill said "Send the Bundle as a transaction so all-or-nothing semantics apply server-side" with **no batch/transaction distinction anywhere in the plugin** (every other `batch` hit is RecordMap or SQL batching). **The row's scoping instruction was right and is now grounded.** Measured with `%DynamicObject`: a `batch` Bundle has the same `resourceType` and the same `entry` array, so every check but `type` passes. And read from the shipped `HS.FHIRServer.DefaultBundleProcessor`: `ProcessBundle` accepts **only** `transaction` and `batch` and throws `InvalidBundleTypeForTransaction` with **HTTP 400** for anything else; `isTransaction` is set to 1 **only** for `transaction`; and `ExecuteBundleMain`'s `TROLLBACK` is guarded by that same test, with **no batch equivalent** — so a failed entry in a batch leaves the successful ones committed behind a `200` and a `batch-response`. **Explicitly NOT measured behaviourally:** no FHIR endpoint is configured in the gate namespace (`HS_FHIRServer.ServiceConfigData` absent, SQLCODE -30), so no failing transaction was posted and rolled back; the table is read from the implementation, and the oracle pins that text as a **drift alarm** so an IRIS upgrade fails a test instead of silently invalidating the section. Those two platform assertions are the only ones no mutation can kill — their subject is IRIS, and a length check on the implementation text is what stops them passing vacuously. The gate caught one on the way in: `#1043 QUIT argument not allowed` for a `Quit <expr>` inside a `Catch` block — §5.20's trap, in ObjectScript rather than a DTL. | v1.81.0 |
| ✅ | Wave 4 **S43** — `recordmap-typed-fields-fixture.cls` + `msg-censo-validated.cls` + `dtl-censo-validate-flags.cls` + `tdd-censo-validate-flags.cls` + deliverable **§1.21**. **Y9's fixture decision: neither of its two options.** A separate map (`Example.RecordMap.CensoTyped`) carries the typed fields, because `Example.RecordMap.Censo` is referenced by §1.11, the `GetObject` sections and its own test — editing a gated artefact to serve a different section is the wrong trade. **And the row's mechanism is wrong for three of four malformations.** Measured: `%Integer` is permissive at parse and strict at save (`GetObject` returns `$$$OK` and stores the literal `ABC`; only `%ValidateObject` objects, `#7207`/`#5802`), so the record DOES become a message; **only `%Date`** matches "never becomes a message", and it raises `<ZODAT>` inside a generated `AdmittedDisplayToLogical` rather than returning a status; and **field count is not checked at all** — too few leaves fields empty, too many silently DISCARDS the extras, with `%ValidateObject` content either way. The date check needs `$ZDateH(v, 5, , , , , , , -1)`: format 5 parses `YYYY-MM-DD`, the NINTH arg suppresses the raise, and an earlier draft with the wrong format and ten arguments **compiled clean** (§6.20 again). **Three harness defects preceded the result, each producing identical output across all six cases:** a `%Stream.GlobalCharacter` where `GetObject` wants `%IO.` (`<PROPERTY DOES NOT EXIST> *Name`), a missing `Rewind()` (`ErrStreamAtEnd`), and a setup spelled **`%OnBeforeAllTests`** — a name `%UnitTest.TestCase` does not declare, so it was dead code and the suite ran with no setup. Grepping that spelling found the **same defect in shipped material**: `tdd-wsdl-reader-signature.cls` (v1.76.0) had a `%OnAfterAllTests` that never ran; fixed here. A fifth: mutation 4 first "removed" a guard by appending a comment, changing the file but not the behaviour — the pre-flight now refuses a mutation that leaves the file byte-identical. **And the release itself caught one:** a `git reset --hard origin/main` run immediately BEFORE branching discarded every tracked-file edit while leaving the new untracked artefacts, so the first commit shipped four files referencing a §1.21 that was not there. C2 and C4 failed in CI and the helper refused to merge. Reset BEFORE doing the work, never between the work and the commit. | v1.80.0 |
| ✅ | Wave 4 **S42** — `msg-dual-representation.cls` + `dtl-fill-both-representations.cls` + `tdd-fill-both-representations.cls` + deliverable **§5.24**. The row's claim holds — fill only the typed side and the legacy `%String` is `""` with length 0, silently — **and two worse failures came out of measuring it.** Reading the collection as a scalar gives `41@%Collection.ListOfDT`: an OREF renders **non-empty**, so a `if value '= ""` guard passes and the object reference travels downstream as data. And the legacy representation is **lossy**: `["PEN|LATEX", "GAUZE"]` serialises to `PEN|LATEX|GAUZE`, read back as THREE items from two, with nothing raising — so the transform now rejects such an item, `$$$ThrowStatus` inside a DTL `<code>` block being measured to fail `Transform()` and leave the target unset. Bridge gotcha: `$ListToString(collection, "|")` raises `<LIST>`; `.Serialize()` first is required. **A surviving mutant changed the sample:** deleting the `$select(Count()=0:"", ...)` empty guard left the suite green, because that guard is INERT (`Serialize()` of an empty collection is `""`); chasing it produced the collision finding and the inert guard was deleted. The gate caught a `#5559` on the way in — `--` inside an XML comment, C12, my own rule — and a brace-in-CDATA diagnosis would have been wrong, since an earlier probe with identical CDATA compiled clean. | v1.79.0 |
| ✅ | Wave 4 **S46** — `tdd-testproduction-bpl.cls` + `bpl-scope-swallow-fixture.cls` + `production-bpl-assertions.cls` + deliverable **§5.23**, and a rewrite of `tdd`'s gated snippet. **The row's premise was RIGHT but for a reason it did not name.** As written — "goes green if the BPL returns any object" — it is false in the plain case: with no fault handler a failed `<call>` returns an **error status AND `$IsObject(resp)=0`**, so both weak assertions fail. The trap needs the **§5.21 swallowing catchall**, which the row never mentions: there the process completes at 9, `SendRequest` returns `$$$OK`, a populated response object comes back, and both assertions pass while the only `<call>` failed. Measured as a differential in one production, so the gap is attributed to the BPL's fault handling and not to BPL, `SendRequest` or `TestProduction` — the propagating shape **is** caught by `$$$AssertStatusOK` alone. Discriminators measured, each against its happy-run control: the response FIELD VALUE (`caught: …` vs `none`, the context initial expression), the BO's `Ens.MessageHeader` at Status **8** while every sibling is 9, and **2** `Ens_Util.Log` rows of `Type = 2` versus **0** on success. Enums read rather than assumed — `LogType` 2 = Error; `MessageStatus` 8 = Error, 9 = Completed. A bonus on the same theme: an `Ens.StringContainer.StringValue` overflow logged `#7201` as an error row while `SendRequest` still returned `$$$OK`. | v1.78.0 |
| ✅ | Wave 4 **S49** — `sql-adapter-headless-probe.cls` + `tdd-sql-adapter-headless.cls` + deliverable **§6.21**. The row's `<INVALID OREF>` claim is **confirmed exactly** and now located: `OnInit()` raises at `initAdapterJG+2^EnsLib.JavaGateway.Common.1`, which names neither the BO, nor `EnsLib.SQL.OutboundAdapter`, nor the `JGService` setting that caused it. **The cause is `JGService` being empty, and it took a controlled run to say so** — a first pass blamed the production, because the error text names `in Production 'X'` even with `IsProductionRunning() = 0` (it reads the last *configured* production). Re-measured with the production stopped: `JGService` untouched raises, `JGService = ""` raises **identically**, a non-empty value returns a proper status. So the empty default is the worse failure. **`OnInit()` is not the headless entry point at all — `Connect()` is**, and it returns a `%Status` naming the DSN. Two more traps: `Disconnect` is **VOID** on both the adapter and `EnsLib.SQL.Common`, so `Do ..Disconnect()` is safe on a never-connected adapter while `Set tSC = ..Disconnect()` raises `<COMMAND>` — natural to get wrong because every other method there returns a status; and `ExecuteQuery` before a successful connect returns a status wrapping `<INVALID OREF>ExecuteQueryParmArray^EnsLib.SQL.Common.1`. Routing is decided by the DSN text alone (`jdbc:` prefix → JDBC + `JDBCDriver`; anything else → an ODBC DSN name, `IM002` if unregistered), so `DSN = "FHIRTEST"` is **not** the local namespace. The row's "declare `#Dim` so typos are compile errors" is dropped — §6.20 falsified it; the pin is the suite. | v1.77.0 |
| ✅ | Wave 4 **S52** — `soap-wsdl-reader-generate.cls` + `tdd-wsdl-reader-signature.cls` + deliverable **§6.20**, and a correction to `soap-bo`. **The row's own mechanism does not exist.** It asked for the arity to be pinned "behind a typed `#DIM`, turning a version-pinned existence claim into a standing gate". Measured, a typed `#Dim` gates **nothing**: wrong arity compiles, a **method that does not exist** compiles, and `Process` called as a `##class()` ClassMethod compiles despite `ClassMethod = 0`. ObjectScript resolves instance calls at run time, so the declared type is documentation. What pins an external API is a test that reads the formal spec **and calls it**. Y8's signature was right but one type short — the fourth parameter is `schemaReader As %XML.Utils.SchemaReader`, not bare. Two facts nobody had written down: `pLocationURL` accepts a **local file path**, which is what makes this testable with no endpoint up, and with `MakeBusinessOperation = 1` the interop classes land in a **`BusOp` sub-package** (`BusOp.<Svc>Soap : Ens.BusinessOperation` plus an `Ens.Request`/`Ens.Response` pair), while the client is `%SOAP.WebClient` and each operation gets a `%SOAP.ProxyDescriptor`. **Mutation 2 survived the first suite** — silently dropping `MakeBusinessOperation` was invisible because nothing asserted its effect, which is the very defect the flag prevents; the differential test exists because the mutant lived. | v1.76.0 |
| ✅ | Wave 4 **S48** — `soap-inbound-onprewebmethod-auth.cls` + `production-soap-authguard.cls` + `tdd-soap-onprewebmethod-deny.cls` + deliverable **§6.19**, and a rewrite of the `business-services` fence. **Y2 was right twice over and the measurement went further than either.** The skill's stated prerequisite is false — `OnPreWebMethod` is a `%SOAP.WebService` callback and fires with the adapter blanked — and the row's unverified mechanism turns out to be **absent**: measured over HTTP, `Quit $$$ERROR(...)` returns **HTTP 200 with the web method executed**, and so does the fence's own body, which has no `Quit` at all. Two fail-open spellings, and they are the two a developer writes first. Only `..ReturnFault(..MakeFault(...))` denies. The dictionary predicted it: **all 52** platform `OnPreWebMethod` methods declare no return type — stored as **NULL**, so `ReturnType = ''` finds none and a naive count calls all 52 typed. Three preconditions each fail in a way that resembles the guard working: no running production (`ErrProductionNotRunning`), an item named `<Tipo>.<Name>` instead of by **class** (`ErrBusinessDispatchNameNotRegistered` — the one place in interop where the naming rule inverts), and a password-protected web app, where Basic authenticates a WSDL GET but a SOAP POST is refused with `wsse:FailedAuthentication` before the method runs. **The suite produced four false results before it produced a true one** — an instance Property that read back empty in the test methods, `ReturnType = ''`, `%Get` on a stream column, and `Base64Encode` without its no-CRLF flag — and every one of them SURFACED AS A REFUSAL. At one point two denial tests were green while every request was failing with `Server Application Error`; only the positive control told them apart. `AutheEnabled = 64` is left explicitly **unverified**: this instance's `UnknownUser` holds no roles. | v1.75.0 |
| ✅ | Wave 4 **S44** — `msg-list-collection-child-table.cls` + `tdd-list-collection-projection.cls` + deliverable **§5.22**. Y4 held, and measuring all four variants on one class turned the row's "naming convention" framing into a sharper finding: **`array` projects a child table by default and `list` does not**. Measured — `list Of %String` stays a `varchar` column on the parent with **no child table**; `list` + `SQLPROJECTION = "table"` projects one whose `element_key` is an **integer** ordinal; a bare `array Of %String` projects one **unasked**, whose `element_key` is a **varchar** key. So the two collection types disagree on both the default *and* the key type, and a query moved from one to the other compiles and then compares an integer to a string. **The harness lied first and the guard is the lasting part:** the runner exported before pushing the classes, so `RunTest/noload` found an empty directory and reported `methods=0 failed=0` — which reads as a pass — with the run id frozen at 324 across the baseline *and* all three mutations. The harness now refuses a verdict whose method count is zero or whose run id did not advance, and the baseline must run before any mutation is attempted. | v1.74.0 |
| ✅ | Wave 4 **S47** — `task-definition-scheduled-bs.cls` + `purge-task-schedule.cls` + `tdd-scheduled-task.cls` + deliverable **§1.20** + a new **BANNED token**. Y3 held on all three counts and the row's suspicion was right: **`NumDaysToKeep` does not exist anywhere** (zero rows, with a positive control returning the two classes carrying `NumberOfDaysToKeep`), `%SYS.Task.Definition` has `OnTask()` and `%SYS.TaskSuper` — which is already BANNED — does not. `NumDaysToKeep` is now banned alongside it, since setting a property that does not exist on a `%SYS.Task` is not an error but a value that goes nowhere. **Two silent defaults found that the row does not mention and that explain the commonest purge complaint:** `Ens.Util.Tasks.Purge` ships `TypesToPurge = "events"` — **not messages** — and `BodiesToo = 0`, so bodies survive a message purge. `KeepIntegrity = 1` is the third and the one to leave alone: a purge that removed nothing on a busy instance is usually that working correctly. **And a two-namespace trap, found by my own bug:** `%SYS.Task` (the schedule) is `%SYS`-scoped while the work class named by `TaskClass` is NOT mapped there, so `Defaults()` hopping to `%SYS` to read `Ens.Util.Tasks.Purge` returned **zero rows with SQLCODE 100** — a clean zero that a `< 0` guard does not catch and that reads as "this class has no settings". Hop for the schedule, stay put for the work. Also measured by round-tripping a real `%SYS.Task`: `TimePeriod` 0=Daily … 5=On Demand, `DailyFrequency` 0=Once/1=Several, and `Settings` is a `$List` of alternating name/value pairs. Suite RUN (**6/6**), mutation-checked five ways; the clean-zero guard's own mutation **survives and is characterised rather than filed as unfalsified** — a combined mutation shows the VALUE assertions catch a bad read, so the guard buys legibility at the read, not detection. C6 then fired on the test's own assertions, which is the guard working: the wrong name is assembled (`"Num" _ "DaysToKeep"`) so the only place the literal appears is a comment explaining it | v1.73.0 |
| ✅ | Wave 4 **S50** — `path-from-setting-and-runtime.cls` + `tdd-path-from-setting.cls` + deliverable **§6.18**. **Recorded late:** this shipped in v1.61.0 without a ledger row, and build-list item 1 kept no `DONE` marker — which is why a later loop firing went looking at it. Both fixed in v1.86.1; the content below is taken from the shipped artefact's own header, not reconstructed. Y8 held: `InstallDirectory` is a ClassMethod on `%SYSTEM.Util` and exists on **no other class in the instance**, and `conformance-review:144` is the only line in the plugin that says so. **Why a parenthesis was not enough:** `##class(%Library.File).InstallDirectory()` **compiles CLEAN** and dies at run time with `<METHOD DOES NOT EXIST>` — so tier 2, and any CI that only compiles, passes a class that cannot run. That is the row's "must be exercised, not merely compiled", and the test calls the wrong form through `$classmethod` and asserts the exception rather than asserting a compile. **And the half CR-10 does not mention is the one that loses data:** once the path is a Setting an operator owns its value, and `NormalizeFilename` treats an **absolute** value as authoritative — the base is discarded. Measured against `InstallDirectory() = /usr/irissys/`: `"mgr/"` → `/usr/irissys/mgr`; `"csp/../mgr"` → `/usr/irissys/mgr`; `"/abs/path"` → `/abs/path`, **escaping the install tree**, where naive concatenation would have produced the nonsense `/usr/irissys//abs/path`. Naive concatenation is wrong in every row and wrong only for some values, so `NormalizeFilename` is the right API — and a component resolving a relative path against the install tree must say whether escaping is allowed and enforce it, which `Resolve` does with the check named rather than implied. | v1.61.0 |
| ✅ | Wave 4 **S45** — `bpl-scope-catchall-compensation.cls` + `production-bpl-scope.cls` + `tdd-bpl-scope-catchall.cls` + deliverable **§5.21**. The row's premise confirmed exactly: `<scope>`, `<faulthandlers>`, `<catchall>`, `<compensationhandlers>` and `<compensate>` appeared **nowhere** in the plugin. **The defect measured end to end, one line apart on the same fixture:** a catchall that records the fault and returns leaves the BUSINESS PROCESS's own header at `Status=9 Completed IsError=0` while the failed call sits beside it at `Status=8` — so the caller is told it worked, and any assertion reading the downstream header or the Event Log passes against it. Adding `set status = $$$ERROR(…)` turns it to `Status=8`. **Five schema facts, each established by COMPILING, because every error message lists what is allowed and never what you need.** `<scope>`'s content model, extracted by feeding the compiler an unknown element, is `(annotation?, ONE activity, compensationhandlers?, faulthandlers)`: **`<faulthandlers>` is REQUIRED** (a scope without one does not compile), `<catchall>` must be inside it, `<compensationhandlers>` must PRECEDE it, and a scope takes exactly one activity. **And the constraint that decides how a handler can be written:** `<compensate>` is legal only as a DIRECT child of `<catchall>` — measured, a `<sequence>` containing it is rejected — so since a catchall takes one activity, **a catchall can compensate, or it can do several things, but not both**; anything a compensating handler must also do belongs in the compensation handler. Plus `initialexpression` is **ObjectScript**, so an unquoted word is `<UNDEFINED>` at `%Construct` with not one activity run, and the full `MessageStatus` enumeration (4=Discarded 5=Suspended 6=Deferred 7=Aborted 8=Error 9=Completed — not the order most people guess). Suite RUN (**6/6**), mutation-checked three ways; **removing `set status` reddens EXACTLY ONE test** while the catchall still fires, still records, and the enumeration is still right — the defect in one line. Two authoring traps hit and recorded on the way: C12 (`--` inside an XML comment, `#5559`) and an assertion copy-pasted from the throwaway probe's BO message rather than the shipped fixture's | v1.72.0 |
| ✅ | **WAVE 3 COMPLETE** (v1.59.0) — and with it **every Sxx row in the map, S1 through S41**. The wave's premise pass (v1.48.0) called 7 of 12 rows wrong or stale and got **two of its own calls wrong**, both retracted while building: **X3** declared S33's `AlreadyReportedPerSession()` fictional when the bank defines it, and **X6** declared S39 blocked when a `New $Namespace` hop reaches `%SYS` fine. Both were the same error — searching one place, failing, and reporting impossibility — and X1 and X5 were earlier instances of it in the same pass. **The premise pass is still worth running** (it re-scoped S38 off an AL1 that cannot show the trap, and saved S34 from closing a rule that was never dangling), but its NEGATIVE findings need the same standard as its positive ones: a "does not exist" or "cannot be done" is a claim about every access path, and one failed path is not evidence for it. Across the wave, **three sections of the deliverable I had written myself were corrected by building against them** (§15.3's mechanism, and X3/X6's rows), which is the strongest argument in the map for gating prose with artefacts | v1.59.0 |
| ✅ | Wave 3 **S39** — `ssl-config-verifypeer-assert.cls` + `tdd-ssl-verifypeer.cls` + deliverable **§11.13**. **X6 IS OVERTURNED — the block was mine and it was wrong.** X6 observed that `Security.SSLConfigs` is absent from the gate namespace (`SQLCODE -30`, and a late-bound call raises `<CLASS DOES NOT EXIST>` — both re-confirmed) and concluded the row's executable assertion "cannot execute where the gate runs". It can: `New $Namespace` + `Set $Namespace = "%SYS"`, and the audit now runs live from the ordinary gate namespace. That is **two** premise-pass findings retracted in two releases (X3, X6), both from treating one failed access path as proof of impossibility. **`New $Namespace` is the load-bearing half:** with it, `before=[FHIRTEST] after=[FHIRTEST]`; without it the namespace **leaks** (`after=[%SYS]`) — and the mutation that drops it reduced a five-method run to **one recorded method**, because the leak follows the test runner out of the first test and derails everything after. **What the platform already enforces:** `Create` with `VerifyPeer=1` and no CA file gives `#986`, a named-but-absent file `#5012`, a file containing junk `#743` — so validation cannot be switched on without a real parseable chain on disk. Which explains the real failure mode: **`VerifyPeer=0` is the path of least resistance**, not a setting someone flipped. Measured on a clean image, **both** shipped configs (`BFC_SSL`, `ISC.FeatureTracker.SSL.Config`) are `Enabled=1` with `VerifyPeer=0` and no CA file, and the audit reports `2 of 2`. **The state worth the whole artefact is `CAFile` set with `VerifyPeer=0`** — a config that looks configured for validation and performs none, which an audit asking "is a CA file configured?" waves through; `Classify` reports that separately as MISLEADING. Also pinned: `VerifyPeer` is a bare `%Library.Integer`, `MINVAL 0`/`MAXVAL 3`, **`VALUELIST` empty**, as are `Security.Datatype.SSLType` and `.BooleanYN` — nothing in the class says which number means Require, hence one named constant. Suite RUN (**5/5**), mutation-checked five ways, **all five reddening**, including a `WHERE 1=0` mutation that produces an audit which ran, hopped and read the right table and found nothing — caught on the EXAMINED count rather than the finding count | v1.59.0 |
| ✅ | Wave 3 **S33** — `tdd-alert-dedup.cls` + `bo-fail-on-demand.cls` + `bo-alert-recorder.cls` + `rul-alert-dedup-fixture.cls` + `production-alert-dedup-fixture.cls` + deliverable **§7.4**, plus a new gate check **C16**. **Built after retracting X3**, which had declared the row's central API fictional: `AlreadyReportedPerSession()` is the BANK'S OWN method, defined in `alert-dedup-functionset.cls`, and already behaved as the row described — I had searched the platform dictionary and read a claim about the plugin's own code as a claim about the platform, for the third time this session (after X1's `RemoveSchema` and X5). **Replaces a six-step manual procedure needing an SMTP server, a mailbox and a human**: the circuit now runs with both ends replaced by recorders. Measured end to end — an `OnMessage` **returning** `$$$ERROR` yields one `Ens.AlertRequest` carrying the returned reason (`ERROR #5001: FailOnDemand was asked to fail: FAIL please`), and a successful message yields **none** (attempts 1, alerts 0). The dedup key measured exactly: first **0**, second **1**, a different message **0** (the positive control), and **`AlreadyReportedErr("BO.OTHER","boom")` = 1** — `SourceConfigName` is accepted and is NOT part of the key, which is §7.2's deliberate trade and is surprising enough to assert. `AlreadyReportedPerSession()` with no `%Ensemble` returns **0**, while the §7.2 **verbatim** form's bare reference raises `<UNDEFINED>` — asserted, since that path loses the alert. And the verbatim purge's `day-1` really does leak: with day−5 and day−1 planted, the bank's `$order` form removes **both** and keeps today's. **C16 exists because of a defect I introduced and the gate could not see:** a zero-width non-joiner (U+200C) landed inside a method NAME and **IRIS compiled it** — tier 2 green, the suite ran, `%UnitTest` still found the method because the name happens to start with `Test`. Any reference to it by name would have failed invisibly. C16 now rejects zero-width, BOM, NBSP and the bidi overrides in any artefact. Suite RUN (**7/7**), mutation-checked six ways; five redden exactly the right test, and **the sixth is a named gap** — moving the dedup rule after the send changes nothing when one failure yields one alert, and asserting it end to end would mean counting alerts across a `seconds\Interval` bucket boundary, which fails twice an hour for no defect | v1.58.0 |
| ✅ | Wave 3 **S32** — `resend-edit-and-resend.cls` + `tdd-resend-edit-and-resend.cls` + `production-resend-fixture.cls` + `bo-message-sink.cls` + deliverable **§12.6** (chapter 12 previously ended at §12.5). X2 was right that the signature is readable from the dictionary, and reading it first surfaced a distinction **neither the row nor the skills make**: `ResendMessage(pHeaderId)` **re-queues the ORIGINAL header** — measured, header 125 resent, the sink received its payload again, and the newest header id was **still 125**, so there is no second row in the Message Viewer to compare against — while `ResendDuplicatedMessage` creates a new header **over the same body row** (original 123/body 18 → new 124/body **18**). The row's central claim is **confirmed exactly and end to end**: editing that shared row makes the ORIGINAL header read the edit, with no error and a perfectly coherent Visual Trace, because the trace faithfully renders the body each header points at and they point at the same one. Same shape as §4.11's QuickStream. **Two further findings:** `pNewBody` does **not** need saving (an unsaved `%ConstructClone()` produced body id 12 and was delivered), so the order that matters is clone-before-edit, not save-before-resend; and what links the pair is the NEW header's `Description = "Resent 123"` plus a shared `SessionId`, while `CorrespondingMessageId` is empty on both and **`Resent` stayed empty on both headers after either API** — so a report that finds resends by filtering on `Resent` returns nothing, which looks exactly like "no resends happened". Also pinned, and now in CLAUDE.md: **`%UnitTest.TestProduction::TestControl()` is a TEST METHOD** that starts the production, waits `MINRUN`, and STOPS it, so the bank's universal `Quit $$$OK` override leaves nothing running — this suite takes the lifecycle over in `OnBeforeAllTests` and waits on `IsProductionRunning()`. **And the process failure worth more than any of it:** a timeout killed the mutation harness mid-mutation, `Set pClone = tBody` stayed on disk, and every later "baseline" was the mutant; three rounds of diagnosis went into explaining a `%ConstructClone` that was never called, and a wrong "measured" comment reached the artefact before the file was read. The harness now has a sha-checked pre-flight and the rule is in CLAUDE.md. Suite RUN (**7/7**), mutation-checked five ways, **all five reddening exactly the test they break** | v1.57.0 |
| ✅ | Wave 3 **S41** — `production-dicom-onstart-associations.cls` + `utl-dicom-association-registrar.cls` + `tdd-dicom-onstart-associations.cls` + deliverable **§15.5**, and **a correction to my own §15.3**. X13's finding held: the platform's `CreateAssociation(pCallingAET, pCalledAET, pTransferSyntaxes)` takes **three** arguments, Calling then Called, and the vendored wrapper's fourth is `pTypeList`. **§15.3, written in the S40 release, named the wrong mechanism.** It blamed a name "matching nothing"; measured, that path is **loud** — a bogus transfer syntax gives `<EnsDICOM>TransferSyntaxNotSupported` with `AETExists=0` and nothing saved, and a blank AE title gives `<EnsDICOM>BadCallingAET`/`BadCalledAET`, each naming **which side** is wrong. The platform validates the values it is handed; what it does not validate is the **content** of what it saved. The silent path is an `AssociationContext` with an empty `PresentationContexts` list: `%ValidateObject` OK, `%Save` OK, `AETExists` **1**. Two routes reach it, and the sharper one is the vendored wrapper's substring match on `pTypeList` — measured against 289 abstract syntaxes: platform default **237** contexts, `("Storage","FIND")` 209, `("Storage")` 197, **`("Storge")` — one typo — 0, returning `$$$OK`.** And the wrapper **deletes the pre-existing context first**, so a typo on a redeploy destroys a working configuration: register `("Storage")` (197), re-run the same pair with `("Storge")`, and the result is 0 and OK. **§15.5 is the new section:** an association context is namespace data, so a production exported and started in a fresh namespace has none and refuses everything; register from `OnStart` (a **ClassMethod**, base implementation empty), verify the COUNT rather than the status, **repair rather than report** (the `If 'AETExists` guard skips creation for ever otherwise), and log the number. **Two mutations changed the artefact rather than the test.** Removing a blank-AET guard left the suite green — the guard's own verdict, since the platform's error is both specific and better, so the guard was **deleted** and the test now asserts the platform's messages. And the AE titles, unavoidably written twice, are now in **one** place (`AssociationPairs()`) with a test that reads the production's XData and requires set equality — mutations renaming a `RemoteAET` in the production, and dropping a pair from the code, each redden it. Suite RUN (**7/7**), mutation-checked seven ways, with **one branch left defensive and unfalsified and said so at the branch** (a rebuild that itself yields zero contexts needs an empty `^EnsDICOM.Dictionary`) | v1.56.0 |
| ✅ | Wave 3 **S34** — `dtl-fhir-redact-quickstream.cls` + `bp-fhir-request-quickstream.cls` + `tdd-fhir-quickstream-payload.cls` + deliverable **§4.11** and **§5.20**. **Built as the QuickStreamId half only, per X4** — `Example.RUL.FhirRouting` was closed at v1.14.0 and is not dangling. X4's other half is confirmed **exactly**: the message's entire public surface is `QuickStreamId`, `Request`, `HSCoreVersion`, `HSMinVersion`, so there is nothing for an `<assign>` to map a body to. **Four silent failures behind that one clause, all measured, all returning `$$$OK`:** (1) `<assign>`ing `QuickStreamId` is a **reference** copy — two headers over one body, and the Visual Trace faithfully shows the same payload on both hops; (2) **`%ExistsId` returns 0 for a stream that plainly exists** — positive control on a fresh stream nobody had deleted: `%ExistsId`=0, `%OpenId` returns an object, the body reads in full, `$D(^HS.Stream("16"))`=11. Structural, not a bug to wait out: `%Dictionary.CompiledStorage` has **zero rows** for `HS.SDA3.QuickStream` and `HS_SDA3.QuickStream` is not a table (`SQLCODE -30`), so a guard written on `%ExistsId` rejects every payload that has ever existed; (3) **`%Save()` does not write the buffer** — after `Write`+`%Save`, both OK, `^HS.Stream(8)` is EMPTY; `Flush()` writes it, and so does `%OnClose` at scope exit, which is what makes it look intermittent. Past `BUFFERLEN` (32000) the reader gets a **partial** body, which reads as truncation rather than a missing flush; (4) **`Clear()` destroys the object** — it empties `GRef` *and* `Id`, the next `Write()` returns `$$$OK`, `Flush()` throws `<SYNTAX>Flush+4^HS.SDA3.QuickStream.1`, and the broken object throws **again** when it is destroyed, after the method has returned, so the caller gets `SQLCODE -400` from a frame that looks fine. Also measured: `%New()`'s `pTemp` defaults to **1**, so the payload lands in `^CacheTemp.HS.Stream` with an id like `T7`; and nothing deletes a QuickStream with its message (`$D` unchanged at 11 across `%DeleteId`, no `%OnDelete` anywhere on the body chain), so purging does not reclaim `^HS.Stream`. **§5.20 is a second finding that cost a compile:** a DTL `<code>` block is spliced inside a `Try`, so `quit $$$ERROR(...)` is `#1043 QUIT argument not allowed` — read out of the generated `.INT`, whose own idiom is `Set tSC=…` then a **bare** `Quit`. Suite RUN (**7/7**), mutation-checked **eight** ways; **two survive and are explained** — `Write()`+`%Save()` stays green because `%OnClose` flushes when `Transform`'s frame exits, so `TestSaveDoesNotFlushTheBuffer` asserts it where it *is* observable, and the single-`Read()` mutation needs a >32KB fixture, closed instead by asserting the 32000 default from the dictionary | v1.55.0 |
| ✅ | Wave 3 **S38** — `dtl-hl7-repeating-segments.cls` + `dtl-obx-to-text.cls` + `msg-obs-summary.cls` + `tdd-hl7-repeating-segments.cls` + deliverable **§2.14**. Re-scoped per X5 onto **OBX in an ORU_R01** rather than the row's AL1: `AL1` is **top-level** in an ADT_A01, so an AL1 example cannot demonstrate the group trap at all. Sub-transform named `ObxToText`, satisfying the row's rename constraint. **The finding is larger than the row's:** in a grouped message the index route is not a fallback for repeats, it is the only route to anything below the top level. Measured on the shipped ORU_R01, **every** message-level path to `PID` fails with an error status — `PID:3.1`, `PID:PatientIdentifierList(1).IDNumber`, `PID:PatientName(1).FamilyName`, plus four group-prefixed spellings — while the index route returns `12345`. **The first draft of the DTL opened with an `<assign>` on one of those paths and shipped an empty `PatientId`**, which is exactly the silent defect the artefact is about, caught by running it. Also confirmed per X5: both `GetSegmentAt("OBX(1)")` and `GetValueAt("OBX(1):3.2")` **set an error status**, so the skills' "no error" framing was wrong and it is silent only for a caller that discards the third by-reference argument. Suite RUN (6/6), mutation-checked five ways — with **one mutation that passed and is not a gap** (`for i = 2:…` skips MSH, which is never an OBX or PID, so it is behaviourally identical), said so in the header | v1.54.0 |
| ✅ | Wave 3 **S35** — `soap-inbound-adapter-service.cls` + `production-soap-inbound.cls` + `tdd-soap-inbound-targets.cls` + deliverable **§6.17**. `EnableStandardRequests` appeared **nowhere** in the bank, as claimed. **X9's caution resolves in SOAP's favour:** `EnsLib.SOAP.Service` extends `Ens.BusinessService, %SOAP.WebService` — **not** `EnsLib.HTTP.Service` — so it inherits no Host copy, and the property exists on exactly two classes: `EnsLib.SOAP.InboundAdapter` (init **0**) and `EnsLib.HTTP.Service` (init **1**, a different family). For SOAP the target is unambiguous. **The finding the row and the skill both miss is a second axis:** `Parameter ADAPTER;` with no value records an **empty** `_Default`, overriding the inherited `EnsLib.SOAP.InboundAdapter` — so there are two modes, and in the blanked one there is no adapter instance for a `Target="Adapter"` setting to land on. `soap-bo:237` is right for adapter mode and inapplicable to the bank's own SOAP service, which is in direct mode; the artefact wires **both** so the difference is readable. Also shipped: the FQCN item-name exception, the first gated production to show it. **Relabelled from silent to loud-but-misdirecting** per the corrections. Two wrong readings during the build came from `SELECT "Default"` — a reserved word returning zero rows with `SQLCODE -12`, caught only because v1.52.1 made the helper print the status, hours after adding it. And my own **C12** caught a `--` in an XML comment I had just scanned by eye and pronounced clean. Suite RUN (7/7), mutation-checked five ways | v1.53.0 |
| ✅ | Wave 3 **S36** — `soap-bo-typed-adapter.cls` + `msg-forecast-request.cls` + `msg-forecast-response.cls` + `tdd-soap-bo-adapter-wiring.cls` + deliverable **§6.16**. **The row framed the exemption as the defect; it is not.** CLAUDE.md states that a snippet naming a class it does not ship "is reported as a placeholder dependency and does not fail the tier — illustrative names are legitimate", so `soap-bo:170` being off `snippets_baseline.json` is the gate working as designed. The real gap was that the PATTERN had no compiled artefact, which this closes; the fence stays as the illustration of the hardcoded form and now points at the gated one. Two related notes corrected: the map said **three** fence classes were off the baseline — measured, `MyApp.BO.WriteCensusToSQL` and `MyApp.BS.RestEntry` are both **in** it now, leaving one, which also retires the S9 concern that "the tier-3 green over the SQL-BO fence is not a green at all". **The finding neither skill states:** there are two ways to reach the generated client and they trade the same thing oppositely — hardcoding it gives typed methods and puts the class name in code; `WebServiceClientClass` (a **setting**, which `soap-bo` never mentions and `security:37` does) puts the name in configuration but types `%Client` as the generic `%SOAP.WebClient`, so operations go late-bound through `$METHOD`. Also pinned: `WebServiceURL` defaults to the **literal string `"<default>"`**, so a `= ""` guard never fires; and typing a SOAP boolean `%Boolean` is worse than comparing it wrongly — the datatype **coerces `"true"` to 0 on assignment**, losing the answer before any test, which the mutation check confirms. Suite RUN (7/7), mutation-checked four ways | v1.52.0 |
| ✅ | Wave 3 **S37** — `http-manual-envelope-bo.cls` + `tdd-http-response-reading.cls` + deliverable **§6.15**. Of the row's four claims, **two hold, one is false and one is right for the wrong reason** — all measured against a REAL `%Net.HttpResponse`, which mattered. **FALSE: `Data.Rewind()` before the first read.** The row said that without it the body "reads empty with `$$$OK`"; measured, the adapter leaves the stream at the START (`AtEnd=0`) and a first read returns content. Rewind matters for a **SECOND** read, which is the real situation and which the test now pins. **WRONG REASON:** `StatusCode` is `MultiDimensional=False`; the guard is needed because an unset response throws `<INVALID OREF>`. **CONFIRMED and stronger:** the boolean trap is worse than stated — `"true"=1` is 0 **and** `"false"=0` is 0, so neither polarity can be tested numerically and `+"true"` is 0. **CONFIRMED:** the two skills really do call `SendFormDataArray` with 3 and 6 arguments, and the unstated difference is whether the endpoint comes from the call or the adapter's setting. Also: the row's CI note ("only with the narrowed Adapter property") rests on a premise §6.12 already disproved — narrowing buys no compile-time check. A first harness using a hand-written stream gave the opposite Rewind answer, the same mistake as §1.17's `%IO.StringStream`. Suite RUN (7/7), mutation-checked five ways | v1.51.0 |
| ✅ | Wave 3 **S30 + S31** — `schema-bootstrap-sqlproc.cls` + `tdd-hl7-schema-registration.cls` + deliverable **§2.13**. Built in dependency order after the map listed them inverted (S31 tests what S30 registers; corrected in §"Wave 3 follow-on"). Closes a real gate gap: `hl7-schemas` carried `ImportSchema`/`RemoveSchema` as **bare `ClassMethod`s in a fence with no host class**, so neither had ever compiled, and the fence hardcoded `C:\path\to\src\…` which C6 bans — now a parameter. **The skill's "a re-import overwrites but does not clean: a renamed segment lingers" does NOT reproduce.** Measured three ways: it cleans — a renamed `ZAL` resolves to `(empty)` with no removal in between, and a dropped MessageType's globals are absent. **What is real is inheritance, not staleness:** a category with `base='2.5'` answers for a type it does not override by falling through, so `ResolveSchemaTypeToDocType(cat,"ADT_A04")` returns **`2.5:ADT_A01`** — not empty, not an error — and the **prefix is the only signal**. Hence the rule: after a re-import verify what you OVERRODE, since additions fail loudly and lost overrides answer plausibly from the base. Also pinned: `pDummy` really is the second parameter's name (X8), and there is no platform `RemoveSchema` (X1, corrected — the row meant the skill's own companion). Suite RUN (7/7), mutation-checked five ways, with one branch left **defensive and unfalsified** and said so at the branch | v1.50.0 |
| ✅ | **Wave 3 prerequisite: deliverable chapter §15 DICOM** (X7) — the chapters ran 6, 7, 9, 11, 12, 13, 14, so C2 failed any `§15.x` and both DICOM rows were blocked. Written from the `dicom` skill, the vendored MIT tree and fresh measurement, deliberately NOT a duplicate of the 19-class snapshot tier 2b already compiles. Four sections, each a thing that costs attempts: **three different methods are called `CreateAssociation`** (platform 3-arg, `Adapter.TCP` 2-arg by reference, and the vendored sample's own 4-arg wrapper — the skill's example calls the wrapper); argument order is `(Calling, Called)` = initiator first; **`AETExists` takes two arguments while the dictionary says none** (X14); an association context with **zero presentation contexts saves clean and returns `$$$OK`**, after which the `OnStart` guard never retries; and the MWL date rules | v1.49.0 |
| ✅ | Wave 3 **S40** — `dicom-mwl-date-functionset.cls` + `tdd-dicom-mwl-date.cls` under **§15.4**. X12 confirmed both halves; the artefact adds what the row did not have: **the trap is the NINTH positional argument of `$ZDATE` too, and I put it in the tenth slot in the first draft.** It compiled, ran, and did nothing — `$ZDate(999999999, 8, , , , , , , , "")` still threw `<VALUE OUT OF RANGE>` while the nine-slot form returns `""`. So the function whose entire purpose was catching that exception was throwing one. Caught by the test written for it, which is now the suite's central assertion. Also pinned: format 5 accepts `2026-09-18` as well as `20260918` (both → 67831), so `IsDicomDate` tests the **shape** as well as the parse. Suite RUN (8/8), mutation-checked six ways | v1.49.0 |
| ✅ | **Wave 3 premise verification pass** — all **12** rows measured before building any, after Wave 1 (5 wrong/stale in 8) and Wave 2 (9 in 13). Found **7 needing correction**, one a hard blocker, and **5 confirmed exact**. Recorded as §"Wave 3 follow-on" (X1–X13) with a prerequisite-ordered list. The three that change what gets built: **`RemoveSchema` does not exist** anywhere in the dictionary (X1), so S30's stated guard is the `^EnsHL7.Schema`/`^EnsHL7.Description` kill pair; **`AlreadyReportedPerSession` does not exist** either (X3), so S33 must be specified before it can be built; and **`Security.SSLConfigs` is not visible from the gate namespace** (`SQLCODE -30`), so S39's *executable* assertion cannot execute where the gate runs — **declared blocked** (X6). A prerequisite surfaced: the deliverable has **no §15 and no DICOM chapter** — nor §8 nor §10 — so C2 fails any `§15.x` and both DICOM rows are gated on writing one (X7). Two stale claims: `Example.RUL.FhirRouting` is **not** dangling (X4, closed at v1.14.0) and S32's method **is** in the dictionary (X2). Cleanest confirmations: `$ZDateH("20260918",5)`=67831 with `-1` in the **ninth** positional slot (X12), and DICOM **is** available (45 classes) with **two unrelated `CreateAssociation` methods** on different classes — the likely source of the skill's contradictory examples (X13). **The S38 correction was applied in this release** at `transformations`: a failing message-level segment path DOES set an error status, and whether a segment is grouped depends on the message type | v1.48.0 |
| ✅ | Wave 2 **S20** — `production-hl7-mllp.cls` + `routing-rule-hl7-mllp.cls` + `tdd-mllp-setting-targets.cls` + deliverable **§1.19**. W4's refinement is the whole artefact: the operation carries **two timeouts on opposite sides** — `ResponseTimeout` on the **Adapter**, `FailureTimeout` on the **Host** — and a wrong `Target` is silent, so the test is a **sweep** over every setting of every item comparing `Target` against where the property actually lives, with an injected bad target as its positive control. Measured split: service `Port`/`LocalInterface` Adapter, `AckMode` Host; operation `Port`/`ResponseTimeout` Adapter, `FailureTimeout`/`ReplyCodeActions` Host. Three defaults overridden deliberately (`FailureTimeout` **-1** = retry for ever; `ReplyCodeActions` **""** = an application NACK suspends instead of erroring; `ResponseTimeout` stated so its relation to `FailureTimeout` is visible). A third file was needed because a rule's `production=` attribute **pins** it — C10 resolves send targets against the production the rule names, so the existing HL7 rule could not be reused. **W18** records the row's stale claim (`FailureTimeout` has been in a gated file since v1.21.0). Suite RUN (5/5), mutation-checked five ways — both wrong-target mutations caught by the sweep | v1.47.0 |
| ✅ | **Wave 2 COMPLETE** (v1.47.0) | — |
| ✅ | Wave 2 **S22** — `production-lifecycle-helper.cls` + `tdd-production-lifecycle.cls` + deliverable **§1.18**. The ⚠ correction was void twice over (V2, then W14), so built as written. W7 confirmed the whole ladder; every signature verified against `%Dictionary.CompiledMethod` as the row asked. **The confirmed defect:** `RecoverProduction()` returns `$$$OK` from a **Stopped** production and leaves it Stopped (and from Running, leaves it Running) — it takes **no arguments**, so a status-only helper emits `{"state":"2","success":true}` about a production that is down. It also does **not** clear a Suspended production; only `CleanProduction` does, which makes "recover" the wrong reflex for state 3 — the one that blocks every start. Measured state codes: **1 Running, 2 Stopped, 3 Suspended, 4 Troubled**. Also pinned: `StartProduction` **defaults to whatever ran last**, so an empty name is refused. **W17** records the one claim that did not reproduce — `StartProduction` returned with the state already Running, so the wait loop ships flagged as defensive. Suite RUN (8/8), mutation-checked four ways, namespace left at state 2 after every run | v1.46.0 |
| ✅ | Wave 2 **S17 + S18** — `recordmap-generate-bootstrap.cls` + `tdd-recordmap-getobject-encoding.cls` + deliverable **§1.17**. **The item found a live defect in this repo:** `Example.RecordMap.Censo` compiled, `Example.RecordMap.Censo.Record` **absent** — compiling a RecordMap does NOT generate its record class, so `production-censo-intake.cls` could not have processed a single record, for every release that shipped the map. No tier could see it (tier 2 compiles files; the missing class is one no file declares) and the runtime error **names the wrong class** (`*Example.Record`, not `…Censo.Record`). Also measured: `GenerateObject` is **not idempotent** (`#5768 Class already exists`, three times running), so a naive bootstrap works exactly once and fails on every redeploy; and for a class that IS compiled but is not a RecordMap the generator says `#5351 Class does not exist`, which is **false**. Two row premises corrected — W1 (the two methods are on two different classes) and **W16** (the `[SqlProc]`-in-zero-artefacts motivation is void; it is in nine). S18's encoding half **scoped down honestly**: the mojibake does not reproduce on an instance whose default already handles UTF-8 (real UTF-8 file → `'Ángel Muñoz'`, 11 chars, both with and without `CharEncoding`), so the test pins the EXPLICIT setting and the deliverable records the host-dependence §1.11 already warned of. Suite RUN (11/11), mutation-checked — with one branch left **defensive and unfalsified**, said so at the branch | v1.45.0 |
| ✅ | Wave 2 **S21** — `bpl-flow-sync-aggregate.cls` + `utl-bpl-sync-audit.cls` + `tdd-bpl-sync-audit.cls` + deliverable **§5.19**. W13 confirmed the elements; the row's premise confirmed exactly: a `<sync calls='AskVendor'>` omitting the second of two async calls **compiles clean**, and so does omitting `<sync>` altogether — the process reaches its end activity with a call outstanding and **completes green**. Since no compile can catch it, the item ships an **audit** that reads `XData BPL` back and names async calls no `<sync>` covers. **The audit was wrong first, in the worst direction:** iterating LINES it reported `OK: 1 async call(s), all awaited` for a two-call process written on one line — a false clean, not a miss it admitted to. Now splits on `<` and reads either quote style; the line-based mutation reddens **6 of 10** tests. Suite RUN (10/10), mutation-checked six ways | v1.44.0 |
| ✅ | Wave 2 **S27 + S28**, folded as the row suggested — `tdd-lifecycle-and-eventlog.cls` + deliverable **§5.18**. **Both rows' premises needed correcting.** S27 said a misspelled *or mis-signatured* callback "compiles and is simply never invoked" — measured, only the **misspelling** is silent (`ran=NO`); an extra *defaulted* argument **still runs** (`ran=1`); and declaring it `ClassMethod` is `#5477` at compile, **loud**. S28 said "if the top node is not the count the body never executes" — the top node **IS** the count (287/287, `$Order` agrees), so the skeletons are fine. What replaced it is sharper: **`GetEventLog`'s window floor is the HIGHER of `baseId` and the `BaseLogId` property**, so `baseId=0` does not mean "from the beginning" and a smaller argument **cannot widen** the window — which matters because `tdd` (correctly) tells you to seed `BaseLogId` from `MAX(ID)`, after which no argument reaches back past it. Suite RUN (7/7), mutation-checked. **Two of my own measurements were wrong first:** the extra-argument case was tested on `OnAfterAllTests`, which runs after every test, so no test could ever see its marker — the experiment's result was forced by its ordering; and one "mutation" deleted an assertion from the test, which can never turn a suite red | v1.43.0 |
| ✅ | Wave 2 **S26** — `bo-local-object-save.cls` + `tdd-local-save-status.cls` + deliverable **§6.14**. W11 confirmed an adapter-less BO compiles; the row's count did not survive: it said "all five [bank BOs] carry an adapter", and resolving `$parameter(cls,"ADAPTER")` over the bank gives **eight**, **seven** with one and one (`JavaGateway`) already without — wrong twice, conclusion intact, because the adapter-less LOCAL SAVE was genuinely absent. **Three silent modes measured, all completing the message:** a discarded `%Save()` status (no row); `OnMessage` returning `$$$OK` for an unmapped type (no row); and no missing-reference guard — which is the quietest, because `pRequest.Address` is `""` and reading `.Street` off it returns `""` with **no** `<INVALID OREF>`, so the BO saves an **empty junk row** and reports success. Also pinned: **`%New()` on an `Ens.BusinessOperation` subclass needs a config name** — the argument-less form returns `""` silently, the caller throws, and %UnitTest records failed methods with **zero failed asserts**. Suite RUN (7/7), mutation-checked four ways — and **one mutation passed at first** because the test's row count was filtered (`WHERE City='Barcelona'`) while the defect writes an EMPTY row: the assertion could not see the row it existed to catch | v1.42.0 |
| ✅ | Wave 2 **S19** — `searchtable-hl7-adt.cls` + `tdd-searchtable-rows-landed.cls` + two `SearchTableClass` settings in `production-hl7-intake.cls` + deliverable **§2.12**. W10 confirmed both halves (setting on 29 `EnsLib.HL7.*` classes, **zero** in the bank). The four join shapes measured on ONE indexed message: correct → **8**, `p.ID = st.PropId` → **0**, `ClassExtent = the subclass` → **0**, no `ClassExtent` → **12** (over-counts across six extents sharing PropId 1). Two silent zeros and an over-count, none an error. Props register into the **BASE** extent, never the subclass — so the natural `ClassExtent = 'Example.Search.Hl7Adt'` returns nothing. Built with **symbolic** paths per §2.11, which works: `[PID:PatientIdentifierList().IDNumber]`, `[PID:PatientName().FamilyName]`, `[MSH:MessageType.MessageCode]`. Headline finding: a symbolic search table indexes **none** of its own props for a DocType-less message (`IndexDoc` errors, 4 vendor rows still land) — so the table looks populated while your column is always empty. Also refined the v1.23.0 naming claim: `GetFieldNameFromNumber` returns empty for repeating fields (3, 5, 11) but that does **not** mean no usable name exists — `PatientName` and `PatientIdentifierList` both resolve. Suite RUN (6/6), mutation-checked five ways | v1.41.0 |
| ✅ | Wave 2 **S23** — `default-settings-audit.cls` + `tdd-default-settings-audit.cls` + deliverable **§1.16**. Premise confirmed exactly (W6) and the "blue in the portal" verification replaced with a report. **Nothing validates an SDS row**: rows naming a non-existent item and a non-existent setting both save with `$$$OK`, 3 of 3 accepted. The finding that shapes the whole design: `FilePath` is **not** in the host class's `GetSettings()` — it is an **adapter** setting, so an audit consulting only the host flags `FilePath`/`FileSpec`/`ArchivePath`/`CallInterval`/`Charset` as orphans, the most commonly set values in a file-based production. Valid set = host ∪ `$parameter(host,"ADAPTER")` settings. Also found: `findIfSettingInThisClass` looks like the right API and is `ClassMethod=False, Private=True, Internal=True`, raising `<METHOD DOES NOT EXIST>` — and `GetSettings` fills a **two-level** array keyed by CATEGORY, so a top-level `$Order` finds nothing. Suite RUN (8/8), mutation-checked four ways | v1.40.0 |
| ✅ | Wave 2 **S25** — `lookup-bootstrap-sqlproc.cls` + `tdd-lookup-bootstrap.cls` + deliverable **§5.17**. **The row's central premise was false** (W14): embedded `&sql` is NOT compile-verified — a column typo and a table typo both compile clean and fail at runtime with `SQLCODE -29`/`-30`, the error itself saying *"compiling embedded cached query"*. What IS real is everything else: measured, a non-transactional reload that fails midway leaves the table holding **1** of its original 2 rows **permanently**, while the same sequence in `TSTART … TROLLBACK` restores the old content intact — and a partial table is the worst outcome, because a missing key returns `""` with `exists = 0`, identical to a key that was never meant to exist. The `lookup-tables` snippet it replaces had **none** of the three protections (no transaction, no `SQLCODE` check, unconditional `Quit "OK"`) and lived as a bare `ClassMethod` fence no tier compiles. Suite RUN (6/6), mutation-checked four ways | v1.39.0 |
| ✅ | Wave 2 **S24** — `tdd-convertdatetime-cheatsheet.cls` + deliverable **§5.16**. The five-row "Verified conversions" table had nothing executing it; **all five rows are exactly right** and now have an oracle. What the table did not say is the shape of failure: `ConvertDateTime` validates the field RANGES and never the CALENDAR, so `31/02/1958` → `1958-02-31`, `29/02/2023` → `2023-02-29` and `31/04/1958` → `1958-04-31` are **reformatted into well-formed non-dates**, while `32/01`, `31/13`, `00/01`, `99/99/9999` and free text are **returned unchanged** — two silent modes with different signatures, and the first survives a regex. Forced a correction to `transformations`: the "raw ObjectScript equivalent" is **not equivalent** — `$ZDATEH` refuses 31 February with `<ILLEGAL VALUE>` where `ConvertDateTime` invents a date. Also pinned: `%Q` is the default for BOTH format arguments, so `ConvertDateTime(x)` returns `x` unchanged. Suite RUN (6/6), mutation-checked five ways, read back with the v1.37.0-fixed reader | v1.38.0 |
| ✅ | Wave 2 **S29** — `msg-maxlen-boundaries.cls` + `tdd-maxlen-truncation.cls` + deliverable **§5.15**. The row asked whether a bare `%String` truncates or rejects; **it rejects on every path and nothing is silent** (`%Save` `#7201`, `%ValidateObject` `#7201`, SQL `INSERT` `SQLCODE=-104`, and the in-memory value keeps all 100 chars). `MAXLEN=""` put under a compiler for the first time — it was the prescribed mitigation with **zero** occurrences in the bank. Found a **second ceiling** no prose had noticed: 3,600,000 chars save fine, but at `$$$MaxStringLength` (3,641,144) `%ValidateObject` says **OK** while `%Save` fails `#5002 <MAXSTRING>` inside generated `%SaveData` — the validator and the saver disagree. Suite RUN (8/8) and mutation-checked five ways, each reddening exactly its own test | v1.37.0 |
| ✅ | **The S13 reader shipped a documented call that could not match** — found by using it on S29's run. `Verdict('Example.Tests.X')` is the form its own doc comment prescribed, and the ID is `<instance>||<suite>||<case>`, so a bare class name is a prefix of nothing: it answered "NO RESULTS … nothing was persisted" for a run that had just passed **8 of 8**. Worse than an error — it sends you to fix a working runner. Also found: `MAX(ID)` picks the wrong run because the ID is a **string** (`'9||…'` > `'13||…'`), and results **accumulate**, so an unscoped reader summed five runs as `methods=40` with a reverted mutation still red. Reader now matches `TestCase.Name`, scopes to `CAST($PIECE(ID,'||',1) AS INTEGER)` DESC, and is positive-controlled (mutated → `FAILED: … (1 of 8)`, shipped → `PASSED 8/8`). §5.14 extended | v1.37.0 |
| ✅ | **Wave 2 premise verification pass** — all **13** rows measured before building any. Found **3 needing correction** (S17's `SaveToClass` is on `EnsLib.RecordMap.Model.Record` with arity 0, not on the RecordMap class, and `GenerateObject` is on a third class; S29's "silently truncates" is **wrong on both halves** — a bare `%String` REJECTS with `#7201` on `%Save`, `%ValidateObject` and SQL `INSERT`, and keeps the full value in memory; S24 confirmed but worse — `ConvertDateTime` reformats `29/02/2023` into `2023-02-29`, an invalid date made to look well-formed) and **10 confirmed exact**, with the measured names recorded so no row is re-derived. Recorded as §"Wave 2 follow-on" with a dependency-ordered list. **The S29 correction was applied in the same release**, at `messages` (2 sites) and `soap-bo` (1) — and deliberately NOT at `business-services:368`, which describes a different mechanism (a RecordMap separator defaulting to `$char(32)`) and is right as written | v1.36.0 |
| ✅ | **Custom inbound adapter** (the corrections' "belongs in wave 1" item, proposed by no wave row) — `adp-scheduler-inbound-adapter.cls` + `bs-scheduled-cron.cls` + `tdd-inbound-adapter-dispatch.cls` under §5.2. Confirmed first: `Ens.InboundAdapter` had **0 hits** in `skills/` and 0 in the bank. Adapter executed and mutation-checked — dispatch removed → `dispatched=0` with `sc=OK`; schedule check removed → fires always. Found on the way: an `Ens.BusinessService` subclass **cannot be `%New()`d** (returns `""`, silently), and `BusinessHost` accepts a non-BS stand-in, which is what makes the adapter testable at all | v1.18.0 |
| ⬜ | everything else below | — |

**Counts are NOT restated here. Run the gate.**

    python3 scripts/validate_examples.py --compile    # artefacts, tier-1 checks, fences, bare/loose
    python3 scripts/validate_skills.py                # skill checks, sizes, the token figure

This paragraph used to carry them — "47 compile-gated classes, 50 artefacts, Tier 1: 9 checks,
Tier 3: 29 fences, 19 bare / 33 loose" — dated `as of v1.15.0`, with a later `Tier 1 is 10 checks as
of v1.21.0` **appended** rather than replacing it. By v1.109.0 every figure was wrong (169 compiled,
107 bank artefacts, 20 tier-1 checks, 36 fences, 17 bare / 32 loose) and the appending is the tell:
a number nobody asserts rots, and the fix is to stop restating it. That is the same defect C9 exists
to catch, which is why C9's own counts live in `scripts/snippets_baseline.json` and not in prose.
**Wave 0 is complete.**

## Two systemic caveats — read before planning from any wave

**1. Tier 2 compiles; it never runs.** Every `%UnitTest` sample proposed below gates its API names,
superclass and assertion *shape* — not its outcome. Where a sample's whole value is the executed
assertion, say so in its header and pair it with a release-step run. The one gated test that has
been *run and mutation-checked* is `ch05_bpl_dtl/tdd-testproduction-dtl.cls`; copy its header
convention.

**2. This map audits missing SAMPLES, not missing FACTS.** It cannot surface a fact the skills never
raise, because there is no gap for an auditor to find. Worked example from the same week: on IRIS
for Health *every* interop-enabled production needs a **Foundation namespace** (AFNS) — the plugin
said nothing about it anywhere, and no sample-coverage audit would have found that. When a wave item
feels thin, ask what the skill fails to *say*, not only what it fails to *show*.

## Provenance

Produced by a 12-agent audit (10 auditors × 2 skills, one synthesiser, one adversarial critic) over
all 20 skills, then corrected by the critic and spot-verified by hand against the repo. Claims
independently re-checked before this was committed: `[SqlProc]` in zero compiled artefacts; `##super`
in exactly one example (and that one teaches the exemption); neither `FunctionSet` marked `[Final]`;
pointer coverage 29/43 at audit time; 3 of 4 dangling `BusinessRuleName` in `.cls`. The first
synthesis ran on a truncated payload (6 of 20 skills) and was re-run from cache — this document is
the full-coverage version.

---

## The waves

## Verdict — the ORIGINAL AUDIT, 2026-09-18. Superseded; kept for why the waves exist

> **Read this as history, not as state.** Every count below was true when the 12-agent audit ran and
> is false now — and it contradicts the progress ledger directly above it, which is the authority.
> The most visible example: this section calls four `BusinessRuleName` values dangling, and the
> ledger's first two rows record those rules written in **v1.14.0**. For live numbers run the gate
> (above). For what has landed, read the ledger.
>
> What changed, as of v1.109.0: **169** compile-gated classes, not 43 — 106 in the bank plus **63**
> moved into `skills/*/assets` in v1.102.0. **18 of 20** skills cite at least one example, not 14,
> and no skill cites zero. The waves are complete: every `S`-row built or explicitly declined
> (**S51** per Y1, the gap did not exist; **S58** per Y7). Wave 0 is complete. The "59 missing or
> ungated samples" is the figure the waves were sized against, and it is spent.
>
> It is kept verbatim below because it is the argument for the waves — the reasoning is still sound
> even where the numbers have moved, and deleting it would leave the wave tables with no stated
> justification.

The bank is **43 compile-gated classes across 9 chapters, cited by 14 of 20 skills** — and six skills (`component-map`, `conformance-review`, `dicom`, `interop`, `lookup-tables`, `report-issue`) cite **zero** bank examples, while four more cite exactly one. Measured against what the skills actually prescribe, coverage is thinner than the file count suggests: **59 distinct canonical samples are missing or ungated**, 16 of them P0. The shape of the gap is consistent and is the shape this repo's own history warns about — the gate is green on the wrong things. Four of the five gated productions set `BusinessRuleName` to a rule class **that does not exist anywhere in the repo** (`Example.RUL.AdtRouting`, `Example.RUL.CensoRouting`, `Example.RUL.FhirRouting`, `Example.Alerting.RUL.AlertRouter`; only `Example.RUL.OrderRouting` ships) and tier 2 compiles all four clean, because a setting value is a string. All four message classes in the bank extend `Ens.Request` alone — the exact form the `messages` skill calls necessary-but-not-sufficient — so the vetted model teaches the shared-extent shape. Zero bank artefacts contain `SearchTableClass`, `ResponseTimeout`, `docCategory`, `..Lookup(`, `TSTART`, or `MAXLEN = ""`; `FailureTimeout` appears once and only in the one file tier 2 skips; `##super` appears once and only in the comment explaining when you *don't* need it. The two largest security/alerting artefacts (§7.1 alert circuit, §11.5 OAuth+LDAP, 207 lines of auth code) are indexed in the README and compiled by nothing, because `validate_examples.py:448` skips every non-`.cls`. **Wave 0 below is free and should ship first: nine of those items are text or rename fixes, not samples at all.**

---

## Wave 0 — the right answer is NOT a new sample (16 items, ~1 day)

These cost nothing to build and several of them remove a defect that a new sample would merely sit next to.

| # | Action | Why it beats a sample |
|---|---|---|
| N1 | Delete/replace the "**No BPL, DTL or routing-rule example yet**" paragraph in `examples/README.md` | It is stale — §5.6/§5.7/§5.8 rows exist 40 lines above it. A reader checking COVERED condition 2 is told the sample does not exist. |
| N2 | Dedupe deliverable headings **§1.7/§1.8/§1.9** (lines 142/150/158 vs 175/195/211) | Tier-1 C2 passes either way; a human cannot tell which section a `/// Rule: §1.7` header means. Blocks clean anchoring for three wave-2 samples. |
| N3 | Cut `alerting/SKILL.md:64-93` to a pointer at the gated `alert-dedup-functionset.cls` | The fence is a **stale fork** that compiles clean in tier 3 (`MyApp.UTL.AlertFilterFunctions` is in `snippets_baseline.json`) and carries two defects the bank version fixed: bare `%Ensemble("SessionId")` → `<UNDEFINED>` → **alert lost**, and a `kill …(day-1)` that leaks every quiet day for ever. No gate compares a snippet to its bank counterpart. |
| N4 | Fix `transformations/SKILL.md:157-160` `MyApp.UTL.FunctionSet` `$ZSTRIP` mask | Tier-3 green on a helper that (per `lookup-tables:81-90`, measured on 2026.1) raises `<FUNCTION>` on every message or returns `""` for every input. Masks are validated at runtime; the compile gate can never catch this. Pairs with S7. |
| N5 | Fix placeholder casing in `business-services/SKILL.md:717-731` (`MyApp.Msg.*` → `MyApp.MSG.*`) | The adapterless-BS fence is already a complete class; the casing makes tier 3 excuse it as a #5373 placeholder dependency instead of compiling it. **Cheapest coverage win in the repo.** |
| N6 | Complete the `DICOM.BS.RESTService` fence (`dicom/SKILL.md:171-192`) | Already inside tier 3 and green — but it is a hollowed copy whose `NewStudy()` is `Quit $$$OK`. Copy it and the endpoint 200s every POST and discards every study. |
| N7 | Wrap `interop/SKILL.md:228` bare `[SqlProc]` ClassMethod in a host class | Moves one of the 20 ungated bare members inside tier 3 with no new bank file. |
| N8 | Rename snippet `MyApp.Search.HL7` (Tipo-last) | C5 would reject it in the bank; tier 3 runs no naming check over snippets, so the gate that would catch it never sees it. |
| N9 ⚠ | Rename snippet `REDSA.RecordMap.Ifa` → `Example.RecordMap.Invoice` | Customer-identifying package name now recorded in `snippets_baseline.json`, against CLAUDE.md's vendor-neutral rule. |
| N10 | **No action — auditor correction.** `fhir/SKILL.md:56` already cites `ch04_fhir/production-fhir-facade.cls` | The `fhir` P0 ("skill points at no bank example") is already closed. The remaining FHIR gap is the dangling rule class (S34). |
| N11 | Fix `soap-bo/SKILL.md:78-83` — `%DeleteId({ID})` → `%DeleteId({Address})` | `{ID}` is the carrier row's id, not the payload reference: it deletes an unrelated row. The gated `messages` fence has it right. The ungated copy is the wrong one. |
| N12 ⚠ | Reconcile the three cross-skill API contradictions by hand | `component-map:44` `%GetValue(tbl,v)` vs `lookup-tables:25` `GetValue(t,k,.v)`; `bpl:121` bare-name FunctionSet call vs `transformations:276` qualified; `business-services:648` `Version+MessageType` vs the gated production's bare `2.5`. BANNED-token enforcement iterates **bank artefacts only**, so wrong prose survives a clean tier 1 forever. |
| N13 ⚠ | At the §7.1 and §11.5 README rows, **write the gap at the gate** (CLAUDE.md #151) | Both are non-`.cls` and compiled by nothing. Until S2/S14 land, the index must not read as coverage it does not have. |
| N14 | Add a tier-1 check: production `BusinessRuleName` / `TargetConfigNames` / `DuplexTargetConfigName` must resolve to a shipped class or to one of the production's own item names | Worth more than any single sample here: it catches all **four** dangling rule names at once, and the DICOM snapshot's broken duplex return path. |
| N15 | Extend tier 2 over `BestPractices/external/**` with its own baseline (or state the non-gating at the `dicom` pointer) | 21 classes / 1738 lines that `dicom` calls "the canonical reference for every pattern here", compiled by no tier and frozen read-only. It has already drifted twice (deprecated JavaGateway item; a duplex target naming an item that does not exist). |
| N16 | Put the `report-issue` helpers in `scripts/` + a pytest, not in the bank | `.sh`/`.py` can never satisfy COVERED (tier 2 is `.cls`-only, `examples_baseline.json` holds class names). A bank row for them would be indexed non-coverage. |

---

## Wave 1 — the silent-failure set (16 samples / 22 files, all P0)

Everything here has `silent_failure: true`. The failure mode in every case is a component that starts green and does nothing, or a check that returns OK having looked at nothing.

| # | File(s) | Skills served | Pins down | CI | Depends on |
|---|---|---|---|---|---|
| S1 | `ch02_hl7v2/routing-rule-hl7-adt.cls` | component-map, bpl, hl7-schemas, conformance-review (CR-5/CR-6) | `EnsLib.HL7.MsgRouter.RuleAssist` + `…RoutingEngine` pairing, `docCategory`/`docName` as accepted constraint names, `Document.{MSH:9.1}` surviving the `evaluateRuleDefinition` generator. **Zero `docCategory` hits in the bank**; the only gated rule is the generic-assist one whose own header says HL7 needs the other pair. Silent: wrong assist matches nothing, router reports no error, messages go nowhere. Closes the dangling `production-hl7-intake.cls:48`. | ✅ | — |
| S2 | `ch07_alerting/production-alert-circuit.cls` + `ch07_alerting/alert-routing-rule.cls` | alerting, component-map, conformance-review (CR-3) | Converts the §7.1 XML into the gate. Item name literally `Ens.Alert`, sink BO `MessageMap` on `Ens.AlertRequest`, `AlertOnError=1` on runtime items / `0` on the alert path, finite `FailureTimeout`. The rule half settles bare-vs-`##class()` FunctionSet calls in rule conditions — **#5490 is a compile-time generator failure, so the gate can adjudicate it**, and today the plugin ships both spellings. Silent: an empty/non-matching alert rule routes nothing; the production is green precisely when it can no longer tell you anything. | ✅ | reuses gated `Demo.FilterAlerts.FunctionSet` |
| S3 | `ch05_bpl_dtl/msg-persistent-leftmost.cls` + `tdd-message-own-extent.cls` | messages, interop | `Extends (%Persistent, Ens.Request)` — leftmost wins. The wrong order compiles, projects SQL and reads properties back correctly, so a compile certifies nothing; the test reads `DataLocation` and asserts `^Example.MSG.<Name>D`, not `^Ens.MessageBodyD`. **All 4 bank message classes are the wrong shape today** (verified), one with a header defending it. | ✅ | ⚠️ needs a message-design heading |
| S4 ⚠ | `ch05_bpl_dtl/msg-persistent-child-delete-cascade.cls` + `dat-address-persistent.cls` + `tdd-delete-cascade.cls` | messages, soap-bo, conformance-review (CR-11) | `%DeleteId` on a **%Persistent** child (the gated fence pair compiles a trigger calling `%DeleteId` on a `%SerialObject`, which has no such method), `{Address}` trigger-field spelling, `Foreach = row/object`. Silent: `Ens.Util.Tasks.Purge` removes the header, child rows accumulate "with no error and nothing in the Event Log". Also exposes `validate_examples.py:549` keying snippets by class name — `MyApp.MSG.PersonReq` is defined twice and one is silently overwritten. | ✅ | ⚠️ message-design heading; N11 |
| S5 | `ch02_hl7v2/dtl-hl7-symbolic-paths.cls` + `ch02_hl7v2/tdd-hl7-fixture-test.cls` | transformations, component-map, tdd, hl7-schemas | Symbolic HL7 field paths under `sourceDocType='2.5:ADT_A01'`: a guessed name "returns `""` with `$$$OK`… the assertion passes on the empty string". The test carries the **negative control** (the plausible CamelCase guess must return `""`) plus `ImportFromString` as a *classmethod*, `$Char(13)` separators, DocType assigned after import. The bank's only DTL is object→object in an HL7-first plugin. | ✅ stock 2.5 schemas | — (is itself the fixture dependency of S1, S12, S36) |
| S6 | `ch05_bpl_dtl/dtl-lookup-and-functions.cls` + `utl-functionset-final.cls` | transformations, lookup-tables, component-map, bpl | `..Func()` vs bare — "the single most expensive one-character mistake in a DTL", and the skill itself says a compile-only gate cannot distinguish them: only a test that calls `Transform()` can. Also `..Lookup(…,default)` returning the default rather than `""`, the qualified form for custom functions, and `[Final]` (**both gated FunctionSets mark nothing Final**, contradicting the rule the skill quotes). | ✅ test seeds its own rows in `OnBeforeAllTests` | anchors at existing §5.7 |
| S7 | `ch05_bpl_dtl/lookup-normalize-functionset.cls` (+ test) | lookup-tables, transformations | The working `NormalizeKey` against the tier-3-**green** broken one. A silent empty key makes every `..Lookup()` miss, and with a `""` default every valid code either throws `InvalidDieta` or ships empty downstream. | ✅ compiles; assertion needs a run | N4; ⚠️ **deliverable has zero lookup-table content** |
| S8 ⚠ | `ch01_production/production-preflight-validator.cls` (+ positive control) | bpl, production-lifecycle | The plugin's own pre-restart gate, which today **returns "OK" from three not-found paths** and whose router `SELECT` matches `EnsLib.MsgRouter.RoutingEngine` only (never an HL7 router) with a `TOP 1` that can pick `Ens.Alert`. Embedded `&sql` makes `Ens_Config.Item` and its columns compile-verified. Ship with a known-bad production that must return ISSUES before an OK is believed. | ✅ | ⚠️ new heading |
| S9 ⚠ | `ch06_adapters/sql-bo-batch-transaction.cls` | business-operations, bpl | `SetAutoCommit(0)` **is** the transaction start (there is no `StartTransaction` — a method these skills prescribed and that did not exist), restore-after-Catch guarded by a flag set only on success, and not overwriting your `%Status` with the rollback's. Silent: a `Quit` leaves the transaction open on a `StayConnected` connection and the next message inherits it. Zero `TSTART`/autocommit artefacts in the bank. | ✅ compiles without a DSN | author from a `%Dictionary.CompiledMethod` read |
| S10 ⚠ | `ch06_adapters/bs-recordmap-service-subclass.cls` | business-services, conformance-review (CR-14) | `##super()` **first** in `OnInit` on a real prebuilt host, and the 3-arg `OnProcessInput` (#5478, compile-enforced; 8 of 18 students hit it). The bank's single `##super` is a comment explaining the case where it is *not* needed. Also adjudicates whether gated `Demo.Vendor.BO.AcceptMessage` is a real CR-14 violation or a false positive of the prescan regex. | ✅ | — |
| S11 | `ch06_adapters/bo-rest-outbound.cls` | component-map, business-operations | `..Adapter.Post(.resp,url,body)` arity against the URL-as-setting shape, the **narrowing** `Property Adapter As EnsLib.HTTP.OutboundAdapter` (without it the call is late-bound — proved by gated `Demo.SAML.BO.SoapClient` compiling clean on three adapter members that do not exist on its declared type), and the non-2xx → fail-the-`%Status` path. Silent: 4xx/5xx with an OK `%Status` marks the message Completed while the far end rejected it. | ✅ | ⚠️ needs §6.10 |
| S12 | `ch02_hl7v2/tdd-hl7-router-validation.cls` | hl7-schemas, tdd | The two-direction assertion: a real message routes **and** a malformed one reaches the `BadMessageHandler`. Case 1 alone "passes on a router that validates nothing" — the plugin's signature defect, in the plugin's own test layer. | ⚠️ compiles; CI does not run it | S5 fixture; already-gated `Example.Productions.Hl7Intake` |
| S13 | `ch05_bpl_dtl/unittest-sqlproc-result-reader.cls` | unit-tests, conformance-review (CR-7) | The `$LISTGET` **method-node** reader against the assert-scanning sibling that reported `passed=2 failed=0` for a class %UnitTest printed `**FAILED**` for — plus the 3-arg `DebugRunTestCase` and a legal qualifier string. Ship with the CR-7 "legacy fallback, prefer `iris_test`" banner so a bank file is not read as a recommendation. | ✅ trivial | — |
| S14 ⚠ | `ch11_security/oauth2-server-validate-ldap.cls` (replacing the `.cls.xml`) | security | Puts **207 lines of auth code** under a compiler for the first time: 3 `%syLDAP` macros, ~10 `%SYS.LDAP` classmethod names/arities, `%OAuth2.Server.Properties` types. Flags two things the header does not: `ValidateClient` returns 1 unconditionally (**any** client secret accepted, tokens issue, nothing logged), and it is a *copy* of `%OAuth2.Server.Validate` (re-patch on every upgrade) where the skill prescribes a *subclass*. | ⚠️ **VERIFY FIRST** — `IncludeCode %syLDAP,%syLDAPFunc,%sySite` may not resolve outside `%SYS`. If it does not, declare non-coverage; do not ship it ungated. | — |
| S15 | `ch01_production/drift-report-disk-vs-namespace.cls` | production-lifecycle | The end-of-every-build check, as a compilation unit — its earlier form shipped **broken** (`#1012` on `Continue:…`) while the bank stayed clean. Both directions: an unmounted `src` reports every class ONLY IN IRIS; a wrong package prefix reports `in sync` having compared nothing. Say at the file that the `%ExecDirect` string is outside the compiler's reach. | ✅ | ⚠️ new heading |
| S16 ⚠ | `ch05_bpl_dtl/tdd-destination-assert.cls` | conformance-review (CR-13), tdd | The one API that makes CR-13 performable ("read, not assumed") — where `interop:176` says `Ens_Config.Item` is **not** queryable (SQLCODE -30) and `bpl:142` queries it. One of them is wrong and nothing compiles either. Assert a file BO's `FilePath` on an already-baselined production, so no external DB is needed. | ✅ | shares the settings-read API with S23 — build together |

**Wave 1 totals: 16 samples, 22 files. CI-blocked: 1 (S14, pending include-resolution check). Heading-blocked: 6 (S3, S4, S7, S8, S11, S15).**

---

## Wave 2 — P1 silent failures, core spine (13 samples)

| # | File(s) | Skills | Pins down | CI |
|---|---|---|---|---|
| S17 | `ch01_production/recordmap-generate-bootstrap.cls` | business-services, interop | `[SqlProc]` on the method (missing → `-149 <PRIVATE METHOD>`, "does not look like one"), `%String` return not `%Status` (a returned `%Status` is lost in HTTP CodeMode — failure indistinguishable from success), `SaveToClass`+`GenerateObject` **atomic in one method** (split → "GenerateObject OK" green with `GetObject` unwritten), `"\x0a"` terminator, and the verified `SELECT Example_UTL.Bootstrap_…` projection spelling. `SqlProc` appears **51 times across 11 skills and in zero compiled artefacts**. | ✅ |
| S18 | `ch01_production/tdd-recordmap-getobject-encoding.cls` | business-services, tdd | `GetObject` on the **map** class not `.Record` (wrong one raises the same `<METHOD DOES NOT EXIST>` and sends you chasing the wrong cause) and `CharEncoding="UTF-8"` on the test stream (default Native returns `?gel` with `$$$OK`; the obvious `$ZCVT` repair double-encodes a correct file). Use `$CLASSMETHOD` + a `%Dictionary.CompiledMethod` existence assert so it stages before the generator has run. | ✅ (dep S17) |
| S19 | **extend** `ch02_hl7v2/production-hl7-intake.cls` + `searchtable-hl7-adt.cls` + `tdd-searchtable-rows-landed.cls` | message-search-debug, component-map | "A search table indexes nothing until it is assigned" — `SearchTableClass`, `Target="Host"`, on service *and* operation, never on a router. **Zero `SearchTableClass` in the bank.** The test is the positive control separating "no data" from "not indexed", and pins the `p.PropId … AND p.ClassExtent` join (the `p.ID = st.PropId` form compiles, returns zero, and reads as missing data). Silent: a `PropId` query returning zero reads as "that patient was never here". | ✅ (⚠️ §12.6 needed for the standalone files; the two extra `<Setting>` lines need nothing) |
| S20 ⚠ | `ch02_hl7v2/production-hl7-mllp.cls` | component-map, bpl, alerting | MLLP Host/Adapter split, a literal `ReplyCodeActions` override (default leaves application NACKs as suspended messages that "look like errors but aren't"), a **finite** `FailureTimeout` (default `-1` = infinite retries; the bank's only `FailureTimeout` is in the file tier 2 skips) and `ResponseTimeout` (0 hits in the bank) with BO < BP ordering. | ✅ (⚠️ N2 first) |
| S21 | `ch05_bpl_dtl/bpl-flow-sync-aggregate.cls` | bpl | `<flow>`/`<sync>` pairing and where aggregation reads responses — the shape `bpl:75` recommends and `bpl:318` forbids improvising, with no template anywhere. Silent: an async `<call>` never awaited lets the process reach its end activity and complete **green** with responses unprocessed. | ✅ |
| S22 ⚠ | `ch01_production/production-lifecycle-helper.cls` (restart + recovery ladder merged) | production-lifecycle | The wait-for-Running loop **attached to the call** (`StartProduction` returns before Running) and the ladder order: status first, classify the refusal, head-check the class, `Recover` vs `Clean` vs stop-that-name. Silent: `action=recover` answers `{"state":"Running","success":true}` on a Suspended production. 105 start refusals across 18/18 students. | ✅ — but method existence is **not** compile-checked (`IDKeyExists()` precedent); verify each against `%Dictionary.CompiledMethod` |
| S23 | `ch01_production/default-settings-audit.cls` | interop, production-lifecycle | `Ens.Config.DefaultSettings` class/property names + an orphan report: every SDS row whose `ItemName`/`HostClassName` matches no item in the production. The archetypal never-read setting. The current verification is "confirm it shows **blue** in the portal" — a colour, in a UI, for a headless plugin. | ✅ §1.4 exists; precedent `credentials-export-reimport.cls` |
| S24 | `ch05_bpl_dtl/tdd-convertdatetime-cheatsheet.cls` | transformations | The five-row "Verified conversions" table, which has nothing behind it. A value that does not match `in` is **returned unchanged**, so `31/02/1958` becomes `1958-02-31` and free text passes through to fail at the JDBC bind, far from the DTL that caused it. This is the oracle that catches the next `ConvertDateTimeToUTC`-shaped prose error. | ✅ trivial |
| S25 | `ch05_bpl_dtl/lookup-bootstrap-sqlproc.cls` (+ accessor method) | lookup-tables, component-map | Embedded `&sql` makes `Ens_Util.LookupTable (TableName, KeyName, DataValue)` compile-verified, and the file carries the transaction bracket the skill prescribes at `:258` and its own snippet omits — during a non-transactional reload every lookup returns `""` with no error. The accessor method settles the three-way contradiction (`GetValue` 3-arg byref / `%GetValue` 2-arg / `FunctionSet.Lookup`). | ✅ (⚠️ lookup heading) |
| S26 | `ch06_adapters/bo-local-object-save.cls` (+ persistent target) | business-operations, component-map | The shape `business-operations` recommends most strongly ("the cleanest BO has no adapter at all") and that no BO in the bank demonstrates — all five carry an adapter. No `Parameter ADAPTER`, MessageMap dispatch, and the `%Status` from `%Save()` **returned, not discarded**. Silent: unchecked save, message Completed, row never written. | ✅ trivial |
| S27 | `ch05_bpl_dtl/tdd-fixture-cleanup-onafteralltests.cls` (fold into S28 if built together) | tdd | Exact `OnBeforeAllTests`/`OnAfterAllTests` names **and signatures**: a misspelled or mis-signatured lifecycle callback compiles and is simply never invoked — the wrong-target setting, transposed to a callback. Cleanup never runs; the next run asserts against stale rows and goes green over them. The bank's only test class has neither callback. | ✅ |
| S28 | `ch05_bpl_dtl/tdd-eventlog-integration-test.cls` | tdd | The shape of the array `GetEventLog` fills. Every skeleton bounds its loop with `For i=1:1:$G(Log)`; if the top node is not the count the body never executes — which, written as an absence assert ("no error was logged"), passes vacuously. | ✅ compiles; CI does not run it |
| S29 | `ch05_bpl_dtl/tdd-maxlen-truncation.cls` | messages, soap-bo | Settles whether a bare `%String` **silently truncates** on `%Save` (asserted twice, in two skills) or rejects with `#7201`. If the run contradicts the skills, **the skills change** — that is the value. Zero `MAXLEN = ""` in the bank, so the prescribed mitigation appears in no compiled artefact. | ✅ (⚠️ no MAXLEN heading) |

---

## Wave 3 — P1 silent failures, protocol & platform periphery (12 samples)

| # | File(s) | Skills | Pins down | CI |
|---|---|---|---|---|
| S30 | `ch02_hl7v2/schema-bootstrap-sqlproc.cls` (custom category carried in `XData`) | hl7-schemas | `EnsLib.HL7.SchemaXML.Import/.Export` and the `^EnsHL7.Schema`/`^EnsHL7.Description` kill pair — the **only** headless schema path, and its plausible neighbour `EnsLib.HL7.Util.SchemaDocument` is recorded as not existing. `RemoveSchema` is the only guard against a re-import that overwrites without cleaning (a renamed segment lingers; the category *looks* re-imported). Carrying the category XML as XData puts the grammar under a compiler that a `.xml` file could never reach. | ✅ — the fence's hardcoded `C:\` path must become a parameter (C6 BANNED) |
| S31 | `ch02_hl7v2/tdd-hl7-schema-registration.cls` | hl7-schemas, tdd | `ResolveSegNameToStructure(cat,"","ZAL",.sc)` / `ResolveSchemaTypeToDocType` arity, and `ZAL:1` vs the group-prefixed path (a DTL `<assign>` that silently yields empty). Forces a superclass declaration — the fence tells the reader to paste into `%UnitTest.TestCase`, which the plugin's own non-negotiable rule forbids. | ✅ (dep S30) |
| S32 | `ch12_monitoring/resend-edit-and-resend.cls` | message-search-debug | `ResendDuplicatedMessage` signature and the clone-then-`pNewBody` order — `docs_introspect` returns `{"methods":[]}` for it, so a compile is the only check available. Silent: a plain resend **shares** the original body, so editing it to "fix" the message rewrites what the original said; the trace stays coherent and the audit trail now lies. | ✅ (⚠️ chapter 12 ends at §12.5) |
| S33 | `ch07_alerting/tdd-alert-dedup.cls` + `ch07_alerting/bo-fail-on-demand.cls` | alerting | Replaces a 6-step manual procedure that needs an SMTP server and a human mailbox. First-call-0 / second-1 / **new-bucket-0** (the positive control), and `AlreadyReportedPerSession()` returning 0 rather than throwing `<UNDEFINED>` outside a host process. The fixture BO pins that an `OnMessage` **returning** `$$$ERROR` is what makes `AlertOnError` emit `Ens.AlertRequest`. Silent: an inverted guard suppresses every alert, and a suppressed alert stream is indistinguishable from a healthy production. | ✅ (dep S2) |
| S34 | `ch04_fhir/rul-fhir-routing.cls` + `ch04_fhir/bp-fhir-request-quickstream.cls` | fhir | Closes the dangling `Example.RUL.FhirRouting` on the newest gated production, and pins that a FHIR DTL works on the Request's **QuickStreamId, not on properties** — the skill's only transformation cross-ref implies property mapping. | ✅ FHIRFOUNDATION |
| S35 ⚠ | `ch06_adapters/production-soap-inbound.cls` | soap-bo | `EnableStandardRequests` on `Target="Adapter"` — the Portal accepts `Target="Host"` silently and the first call dies with `<Ens>ErrSOAPNotEnabled`, naming the setting but not the target. Plus item `Name` = the web-service FQCN or URL dispatch cannot resolve the WSDL. **Zero `EnableStandardRequests` in the bank.** | ✅ wires the already-gated `Example.BS.MyService` |
| S36 ⚠ | `ch06_adapters/soap-bo-typed-adapter.cls` + 2 message siblings | soap-bo | The canonical SOAP BO fence is a complete class that is **absent from `snippets_baseline.json`** — waved through by the #5373 exemption. Puts the narrowing `Property Adapter As EnsLib.SOAP.OutboundAdapter` and `..Adapter.WebServiceURL` (1 occurrence in the entire repo) under a compiler. | ✅ — the siblings must ship or it repeats the exemption |
| S37 | `ch06_adapters/http-manual-envelope-bo.cls` | soap-bo, business-operations | `SendFormDataArray` with explicit `pFormVarNames=""` (the two skills prescribe 3-arg and 6-arg forms of the same call), `Data.Rewind()` before `Read` (without it the response reads **empty with `$$$OK`**), `StatusCode` read inside a `Try` (MultiDim), and SOAP booleans arriving as `"true"`/`"false"` so a `=1` test is false for every response. | ✅ **only with the narrowed Adapter property** — otherwise the call stays late-bound and unchecked |
| S38 | `ch02_hl7v2/dtl-hl7-repeating-segments.cls` + `Example.DT.Al1ToText` | transformations, component-map | Every version of this pattern is currently outside the gate (loose fragments + ```xml fences). The test must assert **three** repeats landed — the positive control the zero-iteration form cannot pass. "The transform completes successfully having processed zero repeats — the worst failure mode, because nothing surfaces." Must show the group fallback (`GetSegmentAt("NK1(N)")` returns empty, no error). | ✅ (dep S5) — **rename constraint:** must not collide with snippet `MyApp.DT.AL1Subtransform` |
| S39 | `ch11_security/ssl-config-verifypeer-assert.cls` | security | Step 1 of §11.7 — "Server Certificate Validation = Require" — the point of the section and the only step with no artefact. With validation `None` the handshake succeeds, everything is green and the peer is unauthenticated for ever. Needs an *executable* assertion because an operator can flip it back by hand after deploy. | ⚠️ **VERIFY** `Security.SSLConfigs` visibility from the gate namespace (%SYS-scoped) |
| S40 | `ch15_dicom/dicom-mwl-date-functionset.cls` | dicom | `$ZDATEH(value, 5)` for `YYYYMMDD` and the missing `-1` error-trap argument. Wrong code → the modality's worklist comes back empty (or no C-FIND response at all) while IRIS shows a green production with the query in the trace. **Best cost/value in DICOM**: pure core ObjectScript, no `EnsLib.DICOM`, compiles on community today. | ✅ code — ⚠️ **the deliverable has no DICOM chapter, so C2 fails until one exists** |
| S41 | `ch15_dicom/production-dicom-onstart-associations.cls` | dicom | `CreateAssociation(pCallingAET, pCalledAET)` is **wire direction** — the skill's two examples use opposite orders and state no rule — and the verify step: a SOP friendly name matching nothing saves a context with **zero presentation contexts** and returns `$$$OK`, after which `AETExists()` is true so the OnStart guard never retries. | ⚠️ **VERIFY** `EnsLib.DICOM.*` + `EnsDICOM.inc` in the gate namespace; DICOM chapter required |

---

## Wave 4 — P2, worth gating once waves 1–3 land (18 samples)

Grouped, one line each. All compile on the community image unless flagged.

- `ch05_bpl_dtl/dtl-fill-both-representations.cls` — *messages + transformations (deduped).* Legacy pipe-string and typed list both filled; a Rich message with only the list reaches the legacy BO as an empty string, silently.
- `ch01_production/dtl-censo-validate-flags.cls` — *transformations.* Validate per record in the DTL, not in RecordMap field types: a record dropped at map level never becomes a message, so Visual Trace shows **nothing at all**. Reuses two already-gated artefacts.
- `ch05_bpl_dtl/msg-list-collection-child-table.cls` — *messages.* Projected child-table naming (`SCHEMA.Class_Property`). Loud (`SQLCODE -30`) but cost 12 round-trips in one cohort.
- `ch05_bpl_dtl/bpl-scope-catchall-compensation.cls` — *bpl.* `<scope>`/`<catchall>`/`<compensationhandler>` occur nowhere in the plugin. A catchall that logs and falls through returns `$$$OK`: a failed downstream call becomes a completed BP with a green trace.
- `ch05_bpl_dtl/tdd-testproduction-bpl.cls` — *bpl, tdd.* Assert returned field **values** and `Ens_Util.Log` rows, not `$IsObject(resp)` — the gated snippet goes green if the BPL returns any object, including a default-constructed one with every `<call>` failed.
- `ch01_production/task-definition-scheduled-bs.cls` + `ch01_production/purge-task-schedule.cls` — *business-services, production-lifecycle, message-search-debug (deduped).* `%SYS.Task.Definition` (not `%SYS.TaskSuper`, which survives an existence check and has no `OnTask`), `PoolSize=1` **and** synchronous calls, and the unverified `NumDaysToKeep` spelling — same species as two existing BANNED findings.
- `ch11_security/soap-inbound-basic-auth.cls` — *business-services.* `OnPreWebMethod` default-deny. The fence's body has no `Quit` at all; copying it fails loud, but the **plausible repair** (`Quit $$$OK`) yields an endpoint that authenticates nothing while looking authenticated.
- `ch06_adapters/sql-adapter-headless-probe.cls` — *business-operations, tdd (deduped).* Adapter `%New()`/`OnInit`/`Disconnect` + `[SqlProc]`, and the JGService precondition whose `<INVALID OREF>` names neither the BO, the adapter, nor JGService. Declare `#Dim` so typos are compile errors.
- `ch06_adapters/path-from-setting-and-runtime.cls` — *conformance-review.* `InstallDirectory` is on `%SYSTEM.Util`, **not** `%Library.File` — asserted in exactly one line in the whole plugin. Must be **exercised**, not merely compiled: the wrong class compiles and fails at runtime.
- `ch03_cda/msg-with-dat-serial-payload.cls` — *conformance-review (CR-9/CR-11).* The `.DAT` package CR-11 makes the default fix has **zero instances** in the bank; all eight `%SerialObject` classes sit in `.MSG.`/`.WSC.`.
- `ch06_adapters/soap-wsdl-reader-generate.cls` — *soap-bo.* `%SOAP.WSDL.Reader.Process` arity behind a typed `#DIM`, turning a version-pinned existence claim into a standing gate.
- `ch11_security/oauth2-client-pkce.cls` — *security + fhir (deduped).* Does **not** inherit the FHIR blocker: `%SYS.OAuth2.*` is core IRIS. A server that never enforces `code_challenge` still issues a token — the integration looks finished and the protection is absent.
- `ch04_fhir/fhirsql-setup-existence-test.cls` — *fhir.* Must be a `%Dictionary.CompiledClass/CompiledMethod` lookup, **not** a call-through: tier 2 excuses `#5373` as a placeholder dependency, so a class calling a nonexistent HS method passes.
- `ch04_fhir/fhir-bundle-transaction-guard.cls` — *fhir.* Scope strictly to validating `Bundle.type` (a `batch` Bundle satisfies the prose and breaks the guarantee); never re-implement transaction semantics.
- `ch15_dicom/production-dicom-query-retrieve.cls` — *dicom.* The duplex wiring is a **quartet**, not a pair, and the snapshot's own response leg names an item that does not exist. Pairs with N14.
- `ch15_dicom/bp-mwl-findresponse-sequence.cls` — *dicom.* N Pending then **exactly one** `Status=0`, including for zero rows; omit it and the modality hangs while IRIS logs N successful sends. ⚠️ DICOM chapter + `EnsLib.DICOM` verification.
- `ch05_bpl_dtl/msg-with-generated-storage.cls` — *interop.* **Contested.** Shows the legitimate post-export `Storage Default` block (zero `^Storage ` blocks in the bank today, and five headers explaining why). Counter-risk: a bank file containing a Storage block invites copy-paste into a new class, which the first half of the same rule forbids, and a BANNED token cannot tell the two cases apart.
- `ch01_production/tdd-namespace-guard-test.cls` — *interop.* **Prefer three added lines in the gated `tdd-testproduction-dtl.cls`** over a new file. Converts "21/21 calls with the schema default USER, 8/8 tests green, nothing in the required namespace" into a red. Honest limit: a sample cannot pin a tool-call parameter — it can only make the wrong-namespace run fail loudly.


---

## Wave 4 follow-on — the premise-verification pass (Y1–Y9), 2026-09-18

All **18** wave-4 rows measured before building any, the fourth such pass. IDs assigned **S42–S59** in
table order, continuing the `Sxx` series — 41 + 18 = 59, which is the map's own total.

**Topic selection was right in all 18 again.** Seven rows needed a correction, and the two DICOM rows'
standing ⚠ is cleared: §15 now exists (§15.1–§15.5, v1.56.0).

| # | row | finding |
|---|---|---|
| **Y1** | S51 `msg-with-dat-serial-payload` | **STALE — the gap does not exist.** The row says "the `.DAT` package CR-11 makes the default fix has **zero instances** in the bank". `ch05_bpl_dtl/dat-address-persistent.cls` declares `Class Example.DAT.Address`, and five more artefacts reference it. The row's *other* half is exact: there are **eight** `%SerialObject` classes in the bank and all eight sit in `.MSG.` (5) or `.WSC.` (3). Same shape as X4 — a row describing a gap that a later release closed. **Do not build it as written**; if anything is left it is a CR-9/CR-11 cross-reference, not a sample. |
| **Y2** | S48 `soap-inbound-basic-auth` | **The row is right and the SKILL IT CITES IS WRONG.** `business-services/SKILL.md:757` says the pattern "requires `EnsLib.SOAP.InboundAdapter`". Measured: `OnPreWebMethod` is a **`%SOAP.WebService`** callback, carried by `EnsLib.SOAP.Service` itself and by 50+ platform classes — no adapter provides it, and per §6.17 the bank's own SOAP service runs with `ADAPTER` blanked. The false prerequisite would send a reader to add an adapter they do not need. Confirmed as stated: the fence body has **no `Quit` at all**. **And one thing is NOT settled from the dictionary:** every platform `OnPreWebMethod` declares **no return type**, while the fence declares `As %Status` — so whether returning an error actually denies the call is unverified, and it is the row's central mechanism. Measure it behaviourally during the build before writing a word about default-deny. |
| **Y3** | S47 `task-definition-scheduled-bs` | **The unverified spelling is wrong, as the row suspected.** There is no `NumDaysToKeep` anywhere; the property is **`NumberOfDaysToKeep`**, on `Ens.Util.Tasks.Purge` and `Ens.Util.Tasks.PurgeMessageBank`. The rest is exact: `%SYS.Task.Definition` extends `%RegisteredObject` and **has `OnTask()`**; `%SYS.TaskSuper` exists (extends `%Persistent,%XML.Adaptor`) and has **no `OnTask`** — so it does survive an existence check, which is the trap. |
| **Y4** | S44 `msg-list-collection-child-table` | **Confirmed and sharper than stated.** Measured on the bank's own `Example.MSG.ObsSummary`, whose `Observations` is a `list Of %String` with **no `SQLPROJECTION`**: only `Example_MSG.ObsSummary` is projected, there is **no child table at all**, and `SELECT * FROM Example_MSG.ObsSummary_Observations` gives `SQLCODE -30, Table 'EXAMPLE_MSG.OBSSUMMARY_OBSERVATIONS' not found`. So the sample is not "here is the naming convention" — it is "the child table exists only if you ask for it", and the default stores a `$List` in one column. Loud, as the row says. |
| **Y5** | S56 `production-dicom-query-retrieve` | **Confirmed exactly, including the dangling name.** The vendored production has 17 items and five `DuplexTargetConfigName` values; four resolve and **`DICOM STOWRS Process` names no item**. Tier 2b compiles the tree clean because a `<Setting>` value is a string — the same defect class as C10/N14, sitting in the *upstream* snapshot. The query/retrieve leg really is more than a pair: `DICOM TCP Out`, `QueryService`, `DICOM Query Process`, `MoveService`, `DICOM Move Process`, `DICOM Store In`, `DICOM Store Process`. |
| **Y6** | S57 `bp-mwl-findresponse-sequence` | **Confirmed, and there is no macro for it.** `$$$Pending` is not defined in `EnsDICOM.inc` (`MPP5610: Referenced macro not defined`). The vendored `WorkListProcess.cls` uses a literal: `SetValueAt(65281, "CommandSet.Status")` with `/* 65281 = "Pending: Optional Keys Supported" */`, and a separate `CreateFinalFindResponse` for the last message. The sample has to name the magic numbers, because the platform does not. |
| **Y7** | S58 `msg-with-generated-storage` | **Facts confirmed, count drifted, and it stays CONTESTED.** Zero `Storage` blocks in the bank — and **six** headers explain why, not five (`cda-from-xsd-persistence-pattern`, `cda-from-xsd-component`, `msg-acceptorder-req`, `msg-acceptorder-rsp`, `msg-persistent-leftmost`, `tdd-message-own-extent`). Six independent headers saying "no Storage block on purpose" is a strong argument that the rule is already carried, and it sharpens the row's own counter-risk: a bank file *containing* a Storage block invites the copy-paste those six headers exist to prevent. **Recommend declining** and moving it to wave 5 unless a reader has actually been misled. |
| **Y8** | S50 / S52 / S53 / S54 / S55 / S45 / S46 / S59 | **Confirmed exactly, no change needed.** `InstallDirectory` is a ClassMethod on `%SYSTEM.Util` and on **no other class in the instance** (S50). `%SOAP.WSDL.Reader::Process(pLocationURL, pPackage="", pTest=0, schemaReader="")` is an **instance** method — the skill already calls it through `%New()`, so the sample's job is gating the arity, and a typed `#Dim` is exactly what makes that a compile-time check (S52). Five `%SYS.OAuth2.*` classes are present, so no FHIR blocker (S53). `validate_examples.py:1127-1139` really does report `#5373` as a placeholder and **not** fail the tier, so a call-through cannot be the existence test (S54). `skills/fhir/SKILL.md:133` says "Send the Bundle as a **transaction**" with no batch/transaction distinction anywhere in the plugin (S55). `<scope>`/`<catchall>`/`<compensationhandler>` appear nowhere but the row itself (S45). `skills/tdd/SKILL.md:426` is verbatim `Do $$$AssertEquals($IsObject(resp), 1, "Got a response back")` (S46). `ch05_bpl_dtl/tdd-testproduction-dtl.cls` exists, so S59's preferred "three added lines instead of a new file" route is open. |
| **Y9** | S43 `dtl-censo-validate-flags` | **Confirmed, and it needs a fixture change the row does not mention.** All four fields of the gated `Example.RecordMap.Censo` are `datatype="%String"`, so nothing can currently be dropped at map level for a type reason — there is no failure to demonstrate. Either add a typed field to the RecordMap (which changes an already-gated artefact and its test) or drive the drop with a malformed record count. Decide before building, and say which. |

**Prerequisite-ordered build list.** Cheapest-and-independent first, decisions before their dependents:

1. ~~**S50**~~ **DONE v1.61.0** — one dictionary fact, exercised rather than merely compiled: the wrong spelling `##class(%Library.File).InstallDirectory()` compiles clean and dies at run time, so the test calls it through `$classmethod` and asserts the exception. Ledger row added late, in v1.86.1.
2. ~~**S45**~~ **DONE v1.72.0.** Premise confirmed; five schema facts established by compiling, and `<compensate>` turns out to be legal only as a catchall's single direct child.
3. ~~**S47**~~ **DONE v1.73.0.** Y3 held; `NumDaysToKeep` is now a BANNED token, and two silent purge defaults (`TypesToPurge="events"`, `BodiesToo=0`) plus a two-namespace clean-zero trap came out of it.
4. ~~**S44**~~ **DONE v1.74.0.** Y4 held and sharpened: the finding is the **asymmetry** — `array` projects by default, `list` does not — plus an `element_key` type split (integer ordinal vs varchar key) that the row never mentioned.
5. ~~**S48**~~ **DONE v1.75.0.** Measured first, as Y2 required — and the mechanism the row assumed does not exist: a returned error status is discarded and the call proceeds. Skill fence rewritten, false prerequisite removed.
6. ~~**S52**~~ **DONE v1.76.0**, ~~**S49**~~ **DONE v1.77.0.** S52 falsified the shared premise — a typed `#Dim` gates neither arity nor method existence — so S49 was re-scoped before building: its pin is the suite, not the declaration. S49's own `<INVALID OREF>` claim held exactly, and the cause turned out to be an empty `JGService`, not the missing production the error text implies.
7. ~~**S46**~~ **DONE v1.78.0.** Premise right, mechanism wrong: the plain failing case DOES fail both weak assertions (`$IsObject(resp)=0`); the trap requires the §5.21 swallowing catchall. Field values, header Status 8 and `Ens_Util.Log` `Type = 2` all pinned against happy-run controls.
8. ~~**S42**~~ **DONE v1.79.0**, ~~**S43**~~ **DONE v1.80.0**, ~~**S55**~~ **DONE v1.81.0**, ~~**S53**~~ **DONE v1.82.0**, ~~**S54**~~ **DONE v1.83.0** — **this line item is COMPLETE.** S42's silent-empty claim held and surfaced two worse failures. S43 answered Y9 with a **third** option. S55 grounded batch-vs-transaction in the shipped `DefaultBundleProcessor`. S53 found IRIS's only PKCE method is **Private**. S54 corrected the row's own reasoning about which tier excuses `#5373`, and corrected §4.9's `Setup(path)` call.
9. ~~**S56**~~ **DONE v1.84.0**, ~~**S57**~~ **DONE v1.85.0** — **this line item is COMPLETE.** S56's upstream defect is now *caught* by a tier-2b ratchet rather than merely reported, and a second unchecked item-reference setting (`OperationDuplexName`) came out of it. S57 confirmed the missing `$$$Pending` and found that `$$$MsgTyp2Str` from the same include does compile.
10. ~~**S59**~~ **DONE v1.86.0** — three added lines in `tdd-testproduction-dtl.cls`, no new file. The obvious "is interop enabled" guard was measured and rejected (`USER` has `Ens.Director`); the guard asserts against the instance default and was proved by running the same suite in two namespaces.
11. ~~**S51**~~ and ~~**S58**~~ **DECLINED v1.86.0** — recorded as decisions, not deferrals. S51's gap does not exist (Y1); S58's counter-risk outweighs its illustration (Y7, and the header count is **six**, not five). **WAVE 4 IS COMPLETE**, and with it every Sxx row in this map — S1 through S59 — is built or explicitly declined.

---

## Wave 5 — declined: prose suffices, or a sample cannot pin it (17 items)

Recording these so a later pass does not re-derive them as gaps.

**Prose is already the denser artefact**
- Identifier `_`/#5559 rule (`interop`, `messages`) — the illegal spelling **cannot compile**, so a bank file can only show the legal one, which all 43 classes already model. Enforcement belongs in the PreToolUse conformance gate (inert on 2 of 3 CLIs — that is an argument about *where* the rule lives, not for a sample).
- CR-8 REST fields typed `%String` — one-line typing rule, wrong type fails loud.
- `bo-file-putline.cls` — bad path errors the message, which sits visibly in the queue; two near-identical file operations already gated.
- `msg-dicom-tags-request.cls` — the message-class shape is gated several times; only the tag strings are DICOM-specific, and those are data.
- `bo-hl7-onrequest-signature.cls` — a wrong `OnRequest` signature fails loudly at dispatch; most of the work is a host class that teaches nothing new.
- `production-future-flow-items.cls` — the rule is complete in prose and its failure is `<CLASS DOES NOT EXIST>` at load; compiling a production **does not resolve item ClassNames**, so the sample would prove nothing.
- `unittest-smoke-testproduction.cls` — a `.cls` cannot pin the value of a global. The real instrument is a positive control in the harness (a deliberately bad `^UnitTestRoot` that must yield zero tests).
- `tdd-boundary-rejection-test.cls` — one macro swap; fold a boundary method into the existing §5.9 test instead of adding a file.
- SAML assertion self-containment test — would reimplement the scope deliberately delegated to `intersystems-ib/SAML-COS`, and its green would not transfer to the partner path.
- `issue-fingerprint-cases.md` — a bad fingerprint produces a visible duplicate issue someone closes.

**A sample is structurally impossible or would be indexed non-coverage**
- `BUILD-ORDER-template.md` — a manifest, not a compilation unit.
- `lookup-seed-genero.csv` — `.csv` carries no `/// Rule:` header and can never enter `examples_baseline.json`. Either drop the two filenames the skill advertises, or let the gated bootstrap SqlProc be the canonical seed.
- `hl7-custom-category.xml` as a standalone bank file — would sit in the bank as text no compiler reads, the state tier 3 exists to eliminate. Carry it as `XData` inside S30 instead.
- `zauthenticate-ldap-expiry` — a `%SYS` `.mac` routine; `.mac` is not in `ARTEFACT_SUFFIXES` and tier 2 is `.cls`-only. **Ship a declared non-coverage row** next to FHIR/DICOM instead.
- `xslt-cda-transform.xsl` — the risk lives outside IRIS, where the skill's own advice sends you; an `.xsl` would be structurally indexed and compile-gated by nothing.
- CR-12 namespace↔src parity — a two-sided tool procedure, not a compilation unit. Belongs in `scripts/` with a test, or in the src-drift hook.
- §6.1.3 `urn` namespace prefix — **the mechanism is unidentified in both the skill and the deliverable.** Do not write a sample on a guess: resolve `%SOAP.WebClient` against a live instance, or declare the non-coverage. An instruction that names an outcome with no method is how an agent invents `..Adapter.%Client.NamespacePrefix`.

---

## Counts and CI flags

| Wave | Samples | Files | All silent-failure or P0/P1? |
|---|---|---|---|
| 0 — not samples (fixes, renames, gate checks) | 16 actions | — | n/a — do first |
| 1 — silent-failure set | **16** | **22** | yes, all P0 |
| 2 — P1, core spine | **13** | ~17 | yes |
| 3 — P1, periphery | **12** | ~16 | yes |
| 4 — P2 | **18** | ~20 | silent in 13 of 18 |
| 5 — declined | 17 | 0 | — |
| **Total new samples proposed** | **59** | **~75** | |

**Cannot be compiled by CI on the community image (explicit flags):**

1. **`ch11_security/oauth2-server-validate-ldap.cls` (S14)** — `IncludeCode %syLDAP,%syLDAPFunc,%sySite` may be `%SYS`-only. Check `%Library.Routine`/`%Dictionary.CompiledClass` in the gate namespace **before** authoring. If it does not resolve: declared non-coverage, not a bank file.
2. **`ch11_security/ssl-config-verifypeer-assert.cls` (S39)** — `Security.SSLConfigs` is `%SYS`-scoped; same check, same fallback.
3. **All four `ch15_dicom/*` samples (S40, S41, wave 4 ×2)** — double-blocked: `EnsLib.DICOM.*`/`EnsDICOM.inc` presence in the gate namespace is unverified, **and the deliverable has no DICOM chapter**, so tier-1 C2 rejects any `/// Rule: §15.x` header today. S40 alone (`$ZDATEH`) needs no DICOM class and clears as soon as a heading exists.
4. **Every non-`.cls` artefact** — `.sh`, `.py`, `.csv`, `.xsl`, `.xml`, `.md`. `validate_examples.py:448` skips them and `examples_baseline.json` holds class names only, so **they can never reach COVERED as defined**. This already affects two *shipped* rows (§7.1, §11.5).
5. **Systemic, applies to all 14 test samples above:** tier 2 **compiles and never runs**. Every `%UnitTest` oracle here gates its API names, superclass and assertion shape — not its outcome. Where a sample's whole value is the executed assertion (S3, S7, S12, S29, S33, S38), say so in its header and pair it with a release-step run, or the green means less than it looks.

**Ordering note.** Build S5 (HL7 fixture) and S23/S16 (production-settings reader) early: four other samples depend on them. Build N14 (the `BusinessRuleName`-resolves check) before wave 1 ships, or S1, S2 and S34 will close three dangling names while the check that would have caught them still does not exist.

---

## Wave 1 follow-on — premises VERIFIED against a live instance, 2026-09-18

S4's premise was false and S5's was half-right, so before building any more of Wave 1 each
remaining row's checkable claim was measured on IRIS for Health 2026.1. **Where this section and a
wave row disagree, this section wins** — and where it disagrees with §"Review corrections", it wins
there too, for the rows named below.

### Corrections found by the verification pass

| # | Claim as written | Measured |
|---|---|---|
| **V1** | `interop` §"is it a table at all": `Ens_Config.Item` is **not a queryable table** (`SQLCODE -30`) | **False — it IS a table.** `SELECT Name, ClassName FROM Ens_Config.Item WHERE Production='<Pkg>.Production'` returns the items, and the count agrees exactly with `tProd.Items.Count()`. The `-30` comes from querying the **class** name (`Ens.Config.Item` → `Table 'CONFIG.ITEM' not found`) — the dots→underscores rule, not a missing table. `Ens_Config.Setting` and `Ens_Util.Queue` genuinely are `-30`; the row lumped them together. **Corrected in `interop` at v1.24.0.** The real hazard is worse than an error: the table is populated for REGISTERED productions, so an uncompiled/undeployed one reads as **0 rows**, which looks like "no items". |
| **V2** | §"Review corrections": S8/S16/S22 must be rewritten against the object API because embedded `&sql` on `Ens_Config.Item` is a compile-time failure | **Void — the reasoning rests on V1.** The table exists, so the `&sql` compiles and works. `bpl:163/178/222` was RIGHT all along, and `bpl:182`'s `##class(Ens.Config.Item).%OpenId(routerItem)` is correct too: it selects `ID`, not a name. Build S8/S16/S22 as originally written. |
| **V3** | `component-map`: `..Adapter.Post(.resp, url, body)` | **Wrong twice.** `Post(*pHttpResponse, pFormVarNames, pData...)` takes **no URL** — it comes from the adapter's `URL` setting. The URL-taking variant is `PostURL(pURL, *pHttpResponse, …)`, URL **first**. The form as written passes the URL as form-var names. **Corrected at v1.24.0**; S11 must assert the arity. |
| **V4** | S7: contrast the working `NormalizeKey` against "the tier-3-**green** broken one" | **Stale.** That broken snippet was fixed in v1.15.0 (wave-0 N4) — `"*-CWE"` raised `<FUNCTION>` and is now `"*WC"` + `$TRANSLATE`. The sample is still worth building; the contrast it was framed around no longer exists in the repo. |
| **V9** | S14: `ValidateClient` returns 1 unconditionally — "**any** client secret accepted, tokens issue, nothing logged" — written as a defect the copy introduced | **Not the copy's doing.** Read out of `%Dictionary.MethodDefinition`, the VENDOR body of `%OAuth2.Server.Validate::ValidateClient` is exactly `Set sc=$$$OK` / `Quit 1`, and the copy reproduces it verbatim. The five copied lines change no behaviour; their cost is ownership (re-diff on every upgrade), not permissiveness. The default does mean this hook accepts every client — but that is IRIS's shipped default, because client authentication happens against the client secret in the server configuration, not here. The defects the copy really introduced are both elsewhere and both measurable: `SupportedClaims` emptied 6 → 0, and a five-formal `ValidateUser` against the vendor's six. Built as a subclass at v1.35.0, which inherits the vendor version of both. |
| **V5** | S13: "the 3-arg `DebugRunTestCase`" | **Wrong arity.** Measured: `DebugRunTestCase(testsuite, testcase, qspec, testmethod, &userparam)` — **5** parameters. And measured separately: it ran without error while writing **no rows** to `%UnitTest_Result.TestMethod`, so it yields no verdict to read back — a zero there is indistinguishable from a pass. S13 must account for that. |

### Premises confirmed (build as written)

| # | Row | Confirmed |
|---|---|---|
| **V6** | S9 | There is **no** `StartTransaction`. `EnsLib.SQL.OutboundAdapter` has `SetAutoCommit(pAutoCommit=1)`, `Commit()`, `Rollback()` — and also `Transact(type)`, which the row does not mention. |
| **V7** | S10 | `EnsLib.RecordMap.Service.FileService.OnProcessInput(pInput As %Stream.Object, *pOutput, &pHint)` is **3-arg**, as the row says, and `OnInit()` exists to `##super()` from. |
| **V8** | S8 | The defect is real and is the router `SELECT`: `ClassName = 'EnsLib.MsgRouter.RoutingEngine'` matches the generic router **only**, never `EnsLib.HL7.MsgRouter.RoutingEngine`, and `TOP 1` picks arbitrarily among several. |

### Suggested order for the rest of Wave 1

Dependency- and cost-ordered, cheapest verified win first:

1. **S12** — HL7 router validation test. Unblocked by S5's fixture (v1.22.0), which it reuses directly.
2. **S6** — `..Func()` vs bare in a DTL. Pure compile-vs-run contrast; the fixture pattern is settled.
3. **S10** — RecordMap service subclass: `##super()` first, 3-arg `OnProcessInput` (V7 confirmed).
4. **S11** — REST outbound BO; must assert the `Post`/`PostURL` arity from V3.
5. **S9** — SQL batch transaction (V6 confirmed; include `Transact`).
6. **S7** — lookup normalisation (re-scoped per V4).
7. **S15** — drift report, disk vs namespace.
8. **S13** — `%UnitTest` result reader (re-scoped per V5; the reader is *more* needed now that `DebugRunTestCase` is known to produce no rows).
9. **S8** and **S16** — both now buildable as originally written (V2), using `Ens_Config.Item`.
10. **S14** — last: the OAuth/LDAP artefact, and per §"Review corrections" ship the **subclass**, never a copy of vendor source.


## Wave 2 follow-on — premises VERIFIED against a live instance, 2026-09-18

Wave 1's pass paid for itself (3 wrong, 1 stale, 1 mis-stated arity out of 8 rows), so the same was
done for all **13** Wave 2 rows before building any of them. **Where this section and a wave row
disagree, this section wins.** Three rows need correcting; the other ten are buildable as written,
with the measured names below so nobody re-derives them.

### Corrections

| # | Claim as written | Measured |
|---|---|---|
| **W18** | S20: "the bank's only `FailureTimeout` is in the file tier 2 **skips**" | **Stale.** That file was the §7.1 alert circuit `.cls.xml`, converted to a compiled class by S2 at v1.21.0 — so `FailureTimeout` has been in a **gated** file since then (`ch07_alerting/production-alert-circuit.cls`, set to 30). The row still stands on its substance: there was no MLLP production, which is the case where an unreachable TCP peer makes the `-1` default routine rather than theoretical. |
| **W17** | S22: the headline is a wait-for-Running loop, because "`StartProduction` returns before Running" | **Did not reproduce.** Measured on the four-item `Example.Productions.CensoIntake`: `StartProduction` returned `$$$OK` and `GetProductionStatus` read **1 (Running)** immediately, with no settle time. It may return early for a large production or slow hosts — unmeasured — so the shipped helper keeps a bounded wait, flagged as **defensive rather than demonstrated**. The rest of the row is confirmed and is where the value turned out to be: `RecoverProduction()` returns `$$$OK` from a **Stopped** production and leaves it stopped, so a status-only verdict claims success about a production that is down. |
| **W16** | S17: "`SqlProc` appears **51 times across 11 skills and in zero compiled artefacts**" | **Void.** Measured at v1.45.0: **nine** bank `.cls` files carry `[SqlProc]` — `recordmap-censo.cls` itself, the drift report, the pre-flight validator, the destination assert, the SDS audit, the lookup bootstrap, the verdict reader, the BPL sync audit and the lookup-bootstrap test. That gap closed while the row sat unbuilt. S17 was built anyway, for a reason the row did not give and which is much stronger: **compiling a RecordMap does not generate its `.Record` class**, and the bank's own map had been shipping without one. |
| **W1** | S17: "`SaveToClass`+`GenerateObject` **atomic in one method**", implying both sit on the RecordMap class | **Right about the pairing, wrong about where they live — and they are on two DIFFERENT classes.** `EnsLib.RecordMap.RecordMap` has neither. Measured: `EnsLib.RecordMap.Model.Record::SaveToClass()` — **arity 0**, on the *Model* class — and `EnsLib.RecordMap.Generator::GenerateObject(pRecordMap, &pTargetClassname, &pStructure=1)`. So the bootstrap method really does have to reach into two places, which strengthens the row's point rather than weakening it. `SaveToClass` also exists on `Ens.Config.Production` and `EnsLib.RecordMap.Model.ComplexBatch` with different signatures — do not pattern-match the name. |
| **W2** | S29: "settles whether a bare `%String` **silently truncates** on `%Save` or rejects with `#7201`" | **It rejects, on every path measured, and NOTHING is silent.** 50 chars saves and reads back 50; 51 gives `ERROR #7201`. Also measured, same property: `%ValidateObject()` alone → `#7201`; SQL `INSERT` → `SQLCODE=-104 Field 'Bare' … failed validation`; and the in-memory value after `Set` is the **full 100 chars**, not truncated. `MAXLEN=""` given 5000 chars saves all 5000. So the mitigation the skills teach is right and the failure mode they describe is wrong, at three sites: `messages:314` ("silently truncated on `%Save` (no error, the data is just gone)"), `messages:320`, `soap-bo:53`. **Two things deliberately NOT swept in.** `business-services:368` ("`Alergias = "Frutos secos\|Marisco"` is silently truncated at `Frutos`") is a **different mechanism** — a RecordMap separator defaulting to `$char(32)`, so the value splits at a space — and stands exactly as written. And the SOAP/XML **import** path was not measured, so whether a wizard-generated property truncates on deserialization is still open; the correction must not claim it. |
| **W14** | S25: "Embedded `&sql` makes `Ens_Util.LookupTable (TableName, KeyName, DataValue)` **compile-verified**" | **False, and it is the row's central premise.** Measured with forced fresh compiles and a positive control proving the detector reports real compile errors (`#1054`): `(TableName, KeyNam, DataValue)` **compiles clean**, and so does `INSERT INTO Ens_Util.LookupTabl`. Both fail at RUNTIME — `SQLCODE -29` (field not found) and `-30` (table not found), the latter saying *"compiling embedded cached query"*, which is the platform explaining that an embedded query is compiled on **first execution**. So `&sql` buys nothing over dynamic SQL for name checking. The rest of the row stands and is now the whole point: the transaction bracket is real and measurable (a non-transactional reload failing midway leaves the table holding 1 of its original 2 rows, permanently), and the only protection against a typo is checking `SQLCODE` and counting the rows loaded. Built at v1.39.0 on the corrected premise. |
| **W3** | S24: a value that does not match `informat` is **returned unchanged**, so `31/02/1958` becomes `1958-02-31` | **Confirmed, and worse than stated. (Refined at v1.38.0: "no validation at any point" was itself too strong — it validates the field RANGES and never the CALENDAR. `32/01/1958`, `31/13/1958` and `99/99/9999` are all returned UNCHANGED; only day 1–31 with month 1–12 is reformatted. Two silent failure modes, not one.)** `ConvertDateTime(value, informat="%Q", outformat="%Q", outf)`, 4 args, on `Ens.Util.FunctionSet` (and ~10 other FunctionSets). Measured: `31/02/1958` → `1958-02-31`; `29/02/2023` → `2023-02-29` (**2023 is not a leap year**); `not a date` → unchanged; an already-ISO `1958-02-31` → unchanged. So a malformed date is not merely passed through, it is *reformatted into a well-formed-looking invalid date*, which is harder to spot downstream than free text. `ConvertDateTimeToUTC` confirmed absent (0 matches) — `transformations:131` already says so, correctly. |

### Premises confirmed (build as written, using these names)

| # | Row | Confirmed |
|---|---|---|
| **W4** | S20 | Exact. `FailureTimeout` InitialExpression = **-1**, origin `EnsLib.HL7.Operation.Standard`; `ReplyCodeActions` = **""**, origin `EnsLib.HL7.Operation.ReplyStandard`. One refinement the row does not state and should use: `ResponseTimeout` = **30** lives on the **adapter** (`EnsLib.HL7.Adapter.TCPOutboundAdapter`) while `FailureTimeout` lives on the **host** — the Host/Adapter split the row wants to teach, sitting right there in the two timeouts it already names. |
| **W5** | S25 | Exact. `Ens.Util.LookupTable` properties are `TableName`, `KeyName`, `DataValue`, and `Ens_Util.LookupTable` is queryable (30 rows present in the gate namespace). |
| **W6** | S23 | Exact. `Ens.Config.DefaultSettings` = `ProductionName`, `ItemName`, `HostClassName`, `SettingName`, `SettingValue`, `Deployable`, `Description`. |
| **W7** | S22 | The whole ladder exists on `Ens.Director`: `RecoverProduction`, `CleanProduction`, `RestartProduction`, `StopProduction`, `GetProductionStatus`, `UpdateProduction`, `IsProductionRunning`. |
| **W8** | S27 | `OnBeforeAllTests` / `OnAfterAllTests` are on `%UnitTest.TestCase` with **no formal parameters** (Origin `%UnitTest.TestCase`, inherited unchanged by `%UnitTest.TestProduction`). The row's "names **and signatures**" is therefore a real hazard: there is no argument to get wrong, so the only way to break it is the spelling. |
| **W9** | S28 | `GetEventLog(type="all", name="", baseId, &v, *New)` — **5** parameters, on `%UnitTest.TestProduction`. |
| **W10** | S19 | Both halves. `SearchTableClass` is a real setting on **29** `EnsLib.HL7.*` classes (services and operations, InitialExpression `""` throughout), and the bank contains **zero** occurrences of it. |
| **W11** | S26 | A `Ens.BusinessOperation` subclass with **no** `Parameter ADAPTER` compiles clean — the adapter-less shape the row wants is legal, as `business-operations` claims. |
| **W12** | S18 | `GetObject` is defined on `EnsLib.RecordMap.RecordMap` and on all seven `EnsLib.RecordMap.Service.*` classes, and **not** on `EnsLib.RecordMap.Base` — which is what the generated `.Record` class extends. So "call it on the map class, not `.Record`" is correct about where it lives. |
| **W13** | S21 | `Ens.BPL.Flow` and `Ens.BPL.Sync` both exist as `Ens.BPL.Activity` subclasses. The attributes to write the sample against: `Ens.BPL.Call` carries `Async` (%Boolean), `Name`, `Target`; `Ens.BPL.Sync` carries `Calls` (the named calls awaited), `Type` (default **"all"**), `Timeout`, `AllowResync` (default 0). The silent failure the row describes is exactly a `<call async="1">` whose name appears in no `<sync calls="…">`. |

### Suggested order for Wave 2

Corrections first where they are cheap, and the two rows that change a skill early, so the skill text
is right before anything is built on it:

1. **S29** — settles a failure mode asserted at three skill sites (W2). Cheapest measurement already
   done; the work is the artefact plus the three corrections.
2. **S24** — same shape, one skill, and the oracle is a table of measured conversions (W3).
3. **S25** — lookup bootstrap; names confirmed exact (W5).
4. **S23** — SDS orphan report; names confirmed exact (W6).
5. **S19** — search table assignment + the positive control separating "no data" from "not indexed".
6. **S26** — the adapter-less BO (W11), which no bank artefact demonstrates.
7. **S27** + **S28** — the two `%UnitTest` lifecycle/verdict rows; the row itself suggests folding them.
8. **S21** — BPL `<flow>`/`<sync>` (W13).
9. **S17** + **S18** — the RecordMap pair, S17 rewritten against the two real classes (W1, W12).
10. **S22** — lifecycle helper (W7).
11. **S20** — MLLP, last: it is the largest, and W4 gives it a second teaching point to fold in.


## Wave 3 follow-on — premises VERIFIED against a live instance, 2026-09-18

Wave 1's pass found 5 wrong or stale premises in 8 rows and Wave 2's found 9 in 13, so all **12**
Wave 3 rows were measured before building any. **Where this section and a wave row disagree, this
section wins.** Seven need correcting — one of them a hard blocker — and five are confirmed exact.

### Corrections

| # | Claim as written | Measured |
|---|---|---|
| **X1** | S30: "`RemoveSchema` is the only guard against a re-import that overwrites without cleaning" | **I misread this, and the row is right.** `RemoveSchema` is not a *platform* method — `SELECT … WHERE Name='RemoveSchema'` over the whole dictionary returns zero rows, which is a useful thing to know so nobody hunts for one — but the row does not claim it is. `hl7-schemas:246` **ships its own** `RemoveSchema(pCategory)` `[SqlProc]` companion, and it does exactly the `^EnsHL7.Schema` / `^EnsHL7.Description` kill pair the row also names. So the guard exists and the row is accurate. **The real gap, which is what S30 should close:** that companion and its `ImportSchema` sibling are **bare `ClassMethod`s in a fence with no host class** — two of the 18 shapes tier 3 cannot compile — so neither has ever been compiled, and the fence hardcodes `C:\path\to\src\Hospital\HL7\PharmacySchema.xml` at `:237`, which C6 bans. Confirmed absent as claimed: `EnsLib.HL7.Util.SchemaDocument`. Confirmed present: `EnsLib.HL7.SchemaXML.Import(pFile, *pCategoryImported, pForceCategory="")`, `.Export(pCategory, pFile)`, and an unmentioned `.GetImportCategory(pFilename)`. *(This is the third time in this pass that I read a claim about the plugin's own code as a claim about the platform — see X5 and X13.)* |
| **X2** | S32: "`docs_introspect` returns `{"methods":[]}` for it, so a compile is the only check available" | **Half wrong — the dictionary has it.** `Ens.MessageHeader::ResendDuplicatedMessage(pOriginalHeaderId, *pNewHeaderId, pNewTarget, pNewBody:%RegisteredObject, …)` is in `%Dictionary.CompiledMethod` with a readable spec, as is `EnsPortal.MessageResendEdit::PerformResend(pHeader, pNewBody, *pNewHeaderId, *pText)`. A compile is not the only check; the signature can be read before writing a line. The row's substance — the clone-then-`pNewBody` order, and that a plain resend SHARES the original body — is untouched and still worth the sample. |
| **X3** | S33: `AlreadyReportedPerSession()` "returning 0 rather than throwing `<UNDEFINED>` outside a host process" | **RETRACTED 2026-09-18 — this finding was wrong.** The method exists; it is the BANK'S OWN, defined in `ch07_alerting/alert-dedup-functionset.cls`, and it already does exactly what the row describes (`$get(%Ensemble("SessionId"))`, `quit:SessionId="" 0`). I searched the **platform** dictionary, found nothing, and concluded the row's API was fictional — the identical mistake to X1 (`RemoveSchema`), and the third time in this session that a claim about the plugin's own code was read as a claim about the platform. S33 needs no re-specification; it needs the test that pins the behaviour. Original finding, for the record: **The method does not exist.** `Name LIKE '%AlreadyReported%'` returns **zero rows** across the entire dictionary. The row's central API is fictional, so the dedup mechanism has to be identified from `Ens.Alerting.*` before this sample can be specified at all — do not build it against the name as written. |
| **X4** | S34: "Closes the dangling `Example.RUL.FhirRouting`" | **Stale — it is not dangling.** `ch04_fhir/routing-rule-fhir.cls` exists and declares `Class Example.RUL.FhirRouting`; it was closed by the v1.14.0 dangling-rule work. The other half is confirmed **exactly** and is where the value is: `HS.FHIRServer.Interop.Request` carries `QuickStreamId` and `Request` (an `HS.FHIRServer.API.Data.Request`) and **no mapped payload properties**, so a FHIR DTL really does work on the stream id. |
| **X5** | S38: "`GetSegmentAt("NK1(N)")` returns empty, **no error**" | **The "no error" half is wrong; the rest holds, and `transformations` was already right.** Measured on 2.5: a failing message-level segment path returns no object **and an error status** — `ERROR <Ens>ErrGeneral: No segment found at path 'OBX(1)'` — and `GetValueAt("OBX(1):3.2")` returns `""` **with an error status** too. So it is silent only if the caller discards the status, which `GetValueAt` invites, the status being its third by-reference argument. That is the sample's real subject. **Two things I had to correct in my own reading of this**, recorded because they nearly became the finding: (a) `transformations:437-464` is substantially CORRECT — it already scopes the failure to *message-level* paths on *grouped* segments, and its measured OBX table matches mine exactly; only the missing error status is additive. (b) My first check used `NK1` in an **ADT_A01**, where NK1 is **top-level**, not grouped — `GetSegmentAt("NK1(1)")` returned an OBJECT and `GetValueAt("NK1(1):2.1")` returned `'SMITH'`, which looked like a refutation and is not one. It does mean the tree's `NK1(N)` line is a poor illustration for readers holding an ADT, since the segment is grouped only in some message types. Build the sample on **OBX in an ORU_R01**, where the grouping is real, and assert the error status. |
| **X6** | S39: ⚠ "VERIFY `Security.SSLConfigs` visibility from the gate namespace (%SYS-scoped)" | **RETRACTED 2026-09-18 — the observation was right and the conclusion was wrong.** The class is indeed absent from an application namespace and `SELECT … FROM Security.SSLConfigs` does give `SQLCODE -30` (re-confirmed, as is `<CLASS DOES NOT EXIST>` from a late-bound call). What was never tried is the namespace hop: `New $Namespace` + `Set $Namespace = "%SYS"` makes the class visible, the table readable, and the audit runnable from the ordinary gate namespace — shipped and RUN at v1.59.0. **Invisible is not unreachable**, and one failed access path is not proof of impossibility; the same error produced X3. Original finding, for the record: **BLOCKED — it is not visible.** `Security.SSLConfigs` is not a compiled class in the gate namespace and `SELECT … FROM Security.SSLConfigs` gives `SQLCODE -30, Table 'SECURITY.SSLCONFIGS' not found`. The row asks for an *executable* assertion, and it cannot execute where the gate runs. Either the sample is declared non-coverage, or it is scoped to something the application namespace can see — do not ship an assertion that cannot run. |
| **X7** | S40 / S41: ⚠ "the deliverable has no DICOM chapter, so C2 fails until one exists" | **Confirmed, and wider than stated.** The deliverable's chapters run 6, 7, **9**, **11**, 12, 13, 14 — there is no §8, no §10 and no §15. So C2 fails any `§15.x`, and a DICOM chapter must be created before either DICOM row can carry a resolvable `/// Rule:` header. This is a prerequisite, not a detail: both rows are otherwise ready (see X12, X13). |

### Premises confirmed (build as written, using these names)

| # | Row | Confirmed |
|---|---|---|
| **X8** | S31 | Exact. `ResolveSegNameToStructure(pSchemaCategory, pDummy As %String = "", pSegName, *pStatus)` — the row's `(cat, "", "ZAL", .sc)` shape is right, and the second parameter is literally named **`pDummy`**, which is worth seeing before guessing at it. `ResolveSchemaTypeToDocType(pSchemaCategory, pTypeName, *pStatus, pDocTypeResolution = "")`. |
| **X9** | S35 | Zero `EnableStandardRequests` in the bank, as claimed. One refinement that changes the artefact: it is a property of **`EnsLib.SOAP.InboundAdapter`** (InitialExpression **0**) — so `Target="Adapter"` is right for a SOAP service — **and also** of `EnsLib.HTTP.Service` (InitialExpression **1**), inherited by `EnsLib.REST.Service`, `EnsLib.HTTP.GenericService` and the HL7 HTTP services, where it is a **Host** property. The correct target therefore depends on the host class; say which, or the example teaches the wrong half. |
| **X10** | S36 | Exact. `snippets_baseline.json` has `expected_clean` (27 entries) and `ungated`; **no** entry is SOAP-related. The fence at `soap-bo:170` (`MyApp.BO.WeatherService Extends Ens.BusinessOperation`) is a complete class that tier 3 extracts and compiles but classifies as a **placeholder dependency**, which is the exemption the row means. `..Adapter.WebServiceURL` appears **once** in real content (`soap-bo:183`). |
| **X11** | S37 | `SendFormDataArray(*pHttpResponse As %Net.HttpResponse, pOp, pHttpRequestIn, pFormVarNames As %String = "", &pData, …)` — `pFormVarNames` is the **fourth** parameter and defaults to `""`, so the row's "explicit `pFormVarNames=""`" is about passing it positionally rather than relying on the default. `SendFormData` and `SendFormDataURL` are the neighbours the two skills confuse it with. |
| **X12** | S40 | Exact, both halves. `$ZDateH("20260918", 5)` = **67831**; `"notadate"` and `"20261332"` raise **`<ILLEGAL VALUE>`** without a trap and return **-1** with the error-trap argument in the **ninth** positional slot. (Bonus: format 5 also accepts `"2026-09-18"`, giving the same 67831.) Blocked only by X7. |
| **X13** | S41 | **DICOM is available** — 45 `EnsLib.DICOM.*` classes in the gate namespace, so the ⚠ is cleared. `EnsLib.DICOM.Util.AssociationContext::CreateAssociation(pCallingAET, pCalledAET, pTransferSyntaxes As %List = $ListBuild($$$IMPLICITVRLETRANSFERSYNTAX))` — **three** parameters, **Calling then Called**. Three distinct methods share that name, which is almost certainly the source of the contradictory examples the row complains about and which the row does not mention: the platform one above; `EnsLib.DICOM.Adapter.TCP::CreateAssociation(&pREQUEST, &pACCEPT)` — same name, unrelated signature, different meaning; and the vendored sample's own wrapper `DICOM.Util::CreateAssociation(pCallingAET, pCalledAET, pTransferSyntaxes, pTypeList)`, which adds a fourth argument so a simulator is not offered every SOP class. The skill's example calls the **wrapper**, so its four arguments are correct there and wrong against the platform method. **CORRECTION TO MY OWN FIRST READING:** I reported `AETExists()` as taking no arguments. It takes **two** — measured, `AETExists("a","b")` returns 0 and `AETExists()` throws `<UNDEFINED>` — see X14 for why the dictionary said otherwise. |
| **X14** | *(method, not a row)* `%Dictionary.CompiledMethod` is authoritative for arity | **Not for index-generated methods, and this invalidated one of my own findings.** `EnsLib.DICOM.Util.AssociationContext` carries an index named `AET`, which generates eight methods — `AETExists`, `AETDelete`, `AETOpen`, `AETCheck`, `AETSQLExists`, … — and **every one of them reports `FormalSpec = ''` and `ClassMethod = False`** in the dictionary. Both are misleading: `AETExists` is callable as a classmethod and requires two arguments. So a dictionary lookup **understates** an index-generated method, exactly as the S22 row's `IDKeyExists()` precedent warned. Whenever a method name looks like `<IndexName><Verb>`, check `%Dictionary.CompiledIndex` for that index and call the method rather than trusting its spec. **And a second-order lesson from the same episode:** my first attempt to check this queried a `Generated` column, which does not exist — `SQLCODE -29, Field 'GENERATED' not found` — and my helper printed the empty row set without the status, so it read as "no index exists". The Atelier response *did* carry the error. Three times this session a bad column name produced a confident zero; print `status.summary` on every query that returns nothing. |

### Suggested order for Wave 3

Prerequisites first, then cheapest verified win:

1. **The DICOM chapter** (X7) — a §15 must exist before S40 or S41 can carry a resolvable header. It gates two rows and blocks nothing else.
2. **S40** — pure core ObjectScript, both halves measured (X12). The cheapest sample in the wave once §15 exists.
3. **S30** — rewritten without `RemoveSchema` (X1), around the global kill pair.
4. **S31** — arities confirmed exact (X8); depends on S30, so it comes after.
   *(These two were listed the other way round when this section was first written, which is a
   dependency inversion: S31 tests what S30 registers. Corrected at v1.50.0.)*
5. **S37** — signature confirmed (X11).
6. **S36** — closes a real gate exemption (X10); its two message siblings must ship with it.
7. **S35** — needs the Host-vs-Adapter distinction from X9 stated, not just `Target="Adapter"`.
8. **S38** — re-scoped per X5: the subject is group nesting, and the skills' "no error" framing is wrong.
9. ~~**S34** — build only the QuickStreamId half (X4); the rule is not dangling.~~ **DONE v1.55.0.** X4 confirmed exactly on the property list; the QuickStreamId half turned out to have **four** silent failure paths behind it (reference copy, `%ExistsId` always 0, `%Save()` does not flush, `Clear()` leaves an object that throws on destruction) plus a DTL `<code>`/`Try` compile rule now at **§5.20**.
10. ~~**S41** — after S40, using the two-`CreateAssociation` finding from X13.~~ **DONE v1.56.0.** X13 held. The row's verify step was right and **§15.3's stated cause was mine to correct** — a bad transfer syntax or blank AE title fails loudly; the silent path is an empty `PresentationContexts` list, reached through the vendored wrapper's `pTypeList` substring match (which also deletes the previous context first). New **§15.5**.
11. ~~**S32** — buildable; read the signature rather than compiling to discover it (X2).~~ **DONE v1.57.0.** X2 held, and reading first surfaced that `ResendMessage` re-queues the ORIGINAL header while `ResendDuplicatedMessage` duplicates it over the same body row. New **§12.6**; chapter 12 had ended at §12.5.
12. ~~**S33**~~ **DONE v1.58.0** — and **X3 WAS MY OWN ERROR, CORRECTED 2026-09-18.** `AlreadyReportedPerSession()` is not a platform API and never claimed to be: the bank's own `ch07_alerting/alert-dedup-functionset.cls` **defines** it, and already returns 0 rather than throwing when `%Ensemble("SessionId")` is absent. X3 searched the platform dictionary and reported the row's API as fictional — the same mistake as X1 (`RemoveSchema`), and the third time this session that a claim about the plugin's own code was read as a claim about the platform. **S33 is buildable as written**: it needs the test that pins the behaviour, not a new specification.
13. ~~**S39** — **declared blocked** (X6) unless re-scoped to something an application namespace can see.~~ **DONE v1.59.0, and X6 IS RETRACTED.** The class really is invisible from an application namespace, and that is not the same as unreachable: `New $Namespace` + `Set $Namespace = "%SYS"` runs the audit live from the gate namespace. Second retraction in two releases (X3, X6), both from treating one failed access path as proof of impossibility. New **§11.13**.


## Review corrections — apply these before building a ⚠ row

The adversarial critic found these after the map was written. Where the map and this section
disagree, **this section wins**.

### Rows built on a premise the plugin contradicts

- **S8, S16, S22 — rewrite against the object API.** `interop/SKILL.md:221` states flatly that
  `Ens_Config.Item` is **not a queryable table** (`SQLCODE -30`): items are embedded objects inside
  the production's XData, and the route is `##class(Ens.Config.Production).%OpenId(…)` then iterate
  `tProd.Items`. These rows promise that embedded `&sql` makes `Ens_Config.Item` "compile-verified"
  and flag themselves ✅ CI. Embedded `&sql` against a nonexistent table is a **compile-time**
  failure, so as written they fail tier 2 rather than verifying anything. `bpl/SKILL.md:161`
  compounds it with `##class(Ens.Config.Item).%OpenId()` on an embedded object — fold that
  contradiction into **N12**, which omits it, and note that C8 cannot see it.
- **S14 — do not copy vendor source.** The proposal is `Alt.OAuth2.Server.Validate`, a verbatim copy
  of `%OAuth2.Server.Validate`. `ch06_adapters/alt-soap-webclient-tracing.cls:8-14` refuses exactly
  this for `Alt.SOAP.WebClient`: *"It is vendor source; the example bank cannot carry it."* Ship the
  LDAP validation as the **subclass** the skill already prescribes — compiles, carries no vendor
  source, sidesteps the `%syLDAP` include question.

### Mis-ranked

- **S35 is not a silent failure.** The map's own cell says the first call dies with
  `<Ens>ErrSOAPNotEnabled`, *naming the setting*. Loud-but-misdirecting. Demote or relabel.
  **RELABELLED at v1.53.0**, and the misdirection turned out to be the whole point: the error names the
  **setting** and not the **target**, so the reader's next move is to set the same thing on the Host,
  which the Portal accepts silently and which changes nothing. The severity is the round trip, not the
  silence — and the mutation that reproduces exactly that move is caught twice by the shipped test.
- **S10 is half-loud.** #5478's 3-arg `OnProcessInput` is **compile-enforced**, so that half is not
  silent. Only the missing `##super()` half is. Keep the sample, drop the P0-silent framing on the
  signature half.
- **Wave 4 `soap-inbound-basic-auth` (§6.3 `OnPreWebMethod`) is P0 and should swap with S14.** Both
  are "auth that authenticates nothing"; this one is cheaper and unblocked — §6.3 exists, the host
  class `EnsLib.SOAP.Service` is already gated (`Example.BS.MyService`), and it needs no `%SYS`
  include check. S14 is the CI-blocked one.
- **S20 needs a verify-first flag.** `ResponseTimeout` has **0 hits in `skills/`**, not just the
  bank — §1.9 names the settings only in Spanish portal labels, so there is no prose to pin. And
  `ResponseTimeout` is an **adapter** setting while `FailureTimeout` is a **Host** setting; a sample
  putting both on `Target="Host"` reproduces the never-read-setting defect. Resolve both targets
  against the live adapter before authoring.

### Component types the map never proposes — all three prescribed, none in bank or skills

- **Custom inbound adapter.** §5.2 already prescribes `Demo.ADP.SchedulerAdapter` for wall-clock
  triggers, and `grep "Extends Ens.InboundAdapter"` over `skills/` returns **zero** — no skill
  teaches the shape. Silent failure is textbook: an `OnTask` that evaluates its cron match and
  returns `$$$OK` without calling `..BusinessHost.ProcessInput()` leaves the service green, ticking
  and producing nothing for ever. §5.2 exists and `ADP` is in `TIPOS`, so no heading work.
  **Belongs in wave 1.**
- **Class-based `Ens.BusinessProcess` (non-BPL).** `bpl:78` recommends it when BPL gets unreadable;
  `tdd:581` records that it returns `""` with no error; CR-1 is a rule *about* the shape. The bank
  has only `Ens.BusinessProcessBPL`. Silent: an `OnRequest` that calls `SendRequestAsync` and
  returns `$$$OK` completes the process before the reply arrives, so `OnResponse` is never reached
  and the trace shows a completed BP. **The whole map treats "BP" as "BPL".**
- **Pass-through / `Ens.StreamContainer`.** Zero hits in `skills/` and zero in the bank, while
  `production-lifecycle:308,351` wires `EnsLib.File.PassthroughOperation` as the canonical alert
  sink — which S2 depends on. The commonest relay shape in the field, undocumented and unmapped.

(Lower value, recorded so it is not re-derived: Managed Alerts `Ens.Alerting.*` — 0 hits anywhere;
§2.9 ordered synchronous chain — no example.)

### Factual

- **Three fence classes are off `snippets_baseline.json`, not one** — `MyApp.BO.WeatherService`
  (S36's target), `MyApp.BO.WriteCensusToSQL` and `MyApp.BS.RestEntry`. They fail as placeholder
  dependencies, so **the tier-3 green over the SQL-BO fence is not a green at all** — matters to S9.
- **Three shipped non-`.cls` rows, not two**: §7.1 `.xml`, §11.5 `.cls.xml`, **and §11.7
  `ssl-trusted-ca-chain.sh`**. **N13** must cover §11.7 too — and for a shell script the right
  remedy is a declared non-coverage row, not a future sample.
- **N9: keep renamed snippets in `MyApp.*`.** `Example.RecordMap.Invoice` moves a snippet into the
  **bank's** namespace, where tier 3 hard-fails on any snippet∩bank collision and C7 resolves
  `Example.` pointers out of the deliverable. `MyApp.RecordMap.Invoice` is the same fix with no
  collision surface.

### Sound, no correction needed

All 34 distinct `${CLAUDE_PLUGIN_ROOT}` pointers across the 20 skills resolve on disk. The
zero-citing (6) and single-citing (4) skill counts, 43 classes / 9 chapters, the
`FailureTimeout`-appears-once claim, and the zero counts for `SearchTableClass` / `docCategory` /
`TSTART` / `..Lookup(` / `MAXLEN = ""` all check out as stated.
